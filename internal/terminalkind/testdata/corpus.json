{
  "$comment": [
    "Terminal-kind behaviour corpus (#306).",
    "",
    "The RULES live in internal/terminalkind/table.json — one ordered table, embedded by Go and",
    "generated into the SDK, interpreted by all three consumers. This file is the other half: what",
    "those rules must MEAN for concrete failure text, with a written argument per row.",
    "",
    "Read by three suites over one file:",
    "",
    "  internal/terminalkind/corpus_test.go                                             (Go)",
    "  packages/nightgauge-sdk/tests/analysis/health/terminalKind.corpusParity.test.ts",
    "  packages/nightgauge-vscode/tests/services/terminalKindSignal.corpusParity.test.ts",
    "",
    "`expected`        what the run record will say — Go's answer, because Go writes the record.",
    "`expected_signal` what the extension may forward to Go over IPC with autonomousComplete, or",
    "                  \"\" to defer. It is the winning rule's kind when that rule is `signal: true`,",
    "                  else a declared signal_extension's kind, else \"\". Both bounds are asserted:",
    "                  a rule joining or leaving the signal subset turns every row it wins red, and",
    "                  a row may differ from `expected` ONLY where a declared extension produces",
    "                  it. That difference is shipped behaviour, not a theoretical allowance — a",
    "                  usage limit naming a model records model_unavailable and reacts",
    "                  rate_limit_quota_exhausted, because the rule that claims it is signal:false.",
    "",
    "HOW TO CHANGE A ROW. `rationale` is mandatory and is the reason the row exists — not a",
    "restatement of `expected`. Editing an expectation means editing the argument for it, in the",
    "same diff, so a classification change has to argue with a written claim rather than update a",
    "string.",
    "",
    "`source`:",
    "  captured  — mined from this machine's live telemetry by",
    "              scripts/capture-terminal-kind-fixture.sh and present verbatim in",
    "              captured-shapes.json. The Go suite enforces that link both ways, so a",
    "              hand-authored string cannot be labelled evidence and a newly observed real shape",
    "              cannot be quietly dropped (#166).",
    "  synthetic — not observed in the captured window, so built from the PRODUCER's own template;",
    "              `producer` names the emitting call site. Synthetic rows extend coverage to",
    "              markers, ordering overlaps and negatives the live population happens not to",
    "              contain; they never stand in for it.",
    "",
    "WHAT THIS FILE DOES NOT HAVE TO DO ANY MORE. Round 2 grew a family of bare-token rows whose",
    "only purpose was satisfying a literal-coverage guard — inputs no producer emits, carrying a",
    "copy-pasted producer sentence that was false for sixteen of the seventeen. Both are gone.",
    "Literal and clause coverage is now structural: internal/terminalkind/stress.go derives an input",
    "for every clause, every term, both edges of every `~` word-bounded term, every ordered rule",
    "pair and every `signal: true` rule composed with every extension clause, straight from the",
    "table, and the answers are committed in testdata/stress-golden.json. A fixture should carry",
    "evidence and argument; deriving mechanical coverage from the table is the table's job.",
    "",
    "WHAT A DERIVED SET STILL CANNOT DO. It composes only the table's own vocabulary, so it never",
    "produces a sentence a real producer would emit — `max_usage limit assertion failed`,",
    "`GitHub API usage limits exceeded` — and a golden can be regenerated by whoever moved the",
    "behaviour. That is what the boundary-negative-* and order-* rows below are for: they say what",
    "the answer must be, in words, for text the machine would never invent."
  ],
  "cases": [
    {
      "id": "captured-api-overloaded",
      "input": "API Error: Overloaded",
      "expected": "api_overloaded",
      "expected_signal": "api_overloaded",
      "source": "captured",
      "rationale": "The Anthropic 529 as the CLI actually surfaces it — a result envelope with is_error:true whose whole message is these three words. It must NOT read as a code fault: api_overloaded routes to a short per-issue backoff with no lifetime-cap increment and no queue pause, while the subagent_crash fallback below would pause the fleet on a capacity blip."
    },
    {
      "id": "captured-api-internal-server-error",
      "input": "API Error: Internal server error",
      "expected": "",
      "expected_signal": "",
      "source": "captured",
      "rationale": "A real is_error envelope that matches nothing. `api error` alone is deliberately not a kind: Go only treats it as transport loss when a specific error code co-occurs, so a bare 5xx falls through to the caller's subagent_crash default. Pinned because it is the honest current behaviour and because it is the negative half of the api_connection_lost gate — a matcher added for `api error` alone would turn this row red."
    },
    {
      "id": "captured-api-connection-refused",
      "input": "API Error: Unable to connect to API (ConnectionRefused)",
      "expected": "",
      "expected_signal": "",
      "source": "captured",
      "rationale": "A real transport failure that classifies as NOTHING: Go's api_connection_lost gate looks for `econnrefused` or `connection refused` (with a space) and this wording has neither — the CLI renders it CamelCase and unspaced. Pinned at the current answer rather than fixed here, because widening the gate changes live recovery routing on the authoritative side; it is listed in this PR's concerns."
    },
    {
      "id": "captured-rate-limit-quota-exhausted",
      "input": "Stage [rate-limit-quota-exhausted] idle 2m 14s after rate_limit_event with overage rejected (five_hour bucket; resetsAt=0) — forcibly terminating process after 4m 0s",
      "expected": "rate_limit_quota_exhausted",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "captured",
      "rationale": "The marker skillRunner stamps when an idle stall coincides with a quota-exhausted rate_limit_event. The kill is stall-SHAPED — the text says `idle` and `forcibly terminating` — so it must be claimed before the stall-kill heuristics or the fleet retries in 30 minutes against a bucket that resets hours later. This row is the reason the rate-limit block outranks stall_kill on every ladder."
    },
    {
      "id": "captured-stall-idle-threshold",
      "input": "Stage exceeded stall idle threshold (12m 0s without output) — forcibly terminating process after 18m 0s (idle for 13m 22s).",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The single most common real kill in the captured window and, before #306, a silent SDK miss: the SDK ladder knew `stall kill threshold` but not `exceeded stall idle threshold`, so the most frequent failure on the machine classified as undefined on one side and stall_kill on the other."
    },
    {
      "id": "captured-stall-kill-threshold",
      "input": "Stage exceeded stall kill threshold (15m 0s) — forcibly terminating process after 15m 0s.",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The sibling wording of the row above, from the same watchdog on a different code path. Both are pinned because the two phrasings differ by one word and a matcher written for either alone leaves the other unclassified."
    },
    {
      "id": "captured-heartbeat-stall",
      "input": "Stage no progress for 1m 19s (heartbeat stall) — forcibly terminating process after 3m 30s.",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The third real stall wording. It carries neither `stall kill threshold` nor `exceeded stall idle threshold`; only the parenthetical `heartbeat stall` identifies it, which is why that literal is load-bearing rather than decorative."
    },
    {
      "id": "captured-budget-exceeded-hard-limit",
      "input": "[BUDGET EXCEEDED] Stage pr-create terminated: $0.18 exceeds hard limit of $0.15",
      "expected": "budget_exceeded",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The most frequently observed shape of all (68 occurrences). It says `hard limit`, one character-class away from the stall ladder's `hard cap` literal — if that literal is ever loosened to `hard limit`, a per-stage budget stop starts reading as a stall and stops counting against the budget the operator set."
    },
    {
      "id": "captured-pipeline-budget-ceiling",
      "input": "[PIPELINE BUDGET CEILING] Pipeline stopped: $107.02 exceeds ceiling of $75.00",
      "expected": "budget_ceiling_hit",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The USD run-level ceiling, and a real ordering trap: lowercased it contains the substring `budget ceiling`, which the generic budget_exceeded block also matches. Go orders budget_ceiling_hit first; the SDK's block carried a comment saying exactly that while sitting BELOW budget_exceeded, so a real ceiling stop was recorded as budget_ceiling_hit and published as budget_exceeded — two different recovery stories for one event."
    },
    {
      "id": "captured-cost-cap-exceeded",
      "input": "[cost-cap-exceeded] Stage feature-dev terminated: cost cap exceeded. Cost $23.0333 exceeded the configured cap ($15.00) after 26m 45s.",
      "expected": "budget_exceeded",
      "expected_signal": "",
      "source": "captured",
      "rationale": "A per-stage cost cap kill. The underlying mechanism is a stall-shaped SIGTERM on a polling tick, so it must be claimed by the cost-cap block BEFORE the stall heuristics — otherwise a run that deliberately spent its cap is retried as a transient stall and spends it again. The SDK had no cost-cap block at all."
    },
    {
      "id": "captured-blocked-dependency",
      "input": "[blocked-dependency] issue #000 deferred — blocked by open dependencies: REDACTED-OWNER/REDACTED-REPO#000 (REDACTED)",
      "expected": "blocked_dependency",
      "expected_signal": "",
      "source": "captured",
      "rationale": "A deferral, not a failure: no lifetime-cap increment, no cascade feed, board stays Ready. Real text, and the reason the marker must beat the generic ladder — the word `deferred` matches nothing, so without the marker this non-failure would land in the caller's subagent_crash default and count against the issue."
    },
    {
      "id": "captured-runaway-progress",
      "input": "[runaway-progress-exceeded] Stage feature-validate terminated: cost $0.0000 below activation threshold $0.5. Cost $0.0000, signals seen: 117, ms since last progress: 0. Treated as transient (stall-kill path). (Issue #000)",
      "expected": "runaway_progress",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The progress-based runaway monitor. Its own text says `stall-kill path`, and it is recovered like a stall, but it keeps a distinct kind so the monitor's own false-positive rate stays measurable — collapsing it into stall_kill would erase the only signal that the monitor is firing on $0.00 runs."
    },
    {
      "id": "captured-containment-breach",
      "input": "[stage:worktree-containment] Stage feature-dev wrote outside its worktree into 1 REDACTED-OWNER/REDACTED-REPO it does not own.",
      "expected": "containment_breach",
      "expected_signal": "",
      "source": "captured",
      "rationale": "A breaching stage exits 0 and reports success, so nothing else marks the run failed. Matched before the stage-behaviour kinds because premature_turn_end would otherwise claim the same exit-0 shape and describe the wrong problem to the operator."
    },
    {
      "id": "captured-adapter-auth-failed-bare",
      "input": "[pipeline-start-failure] adapter-auth-failed: claude-headless=auth probe timed out after 5s",
      "expected": "adapter_auth_failed",
      "expected_signal": "adapter_auth_failed",
      "source": "captured",
      "rationale": "The wrapped pipeline-start form, which carries the marker BARE — no brackets, no underscore. Go matched all three spellings and the SDK mirror matched only the bracketed and underscored ones, so this real, observed shape classified as adapter_auth_failed on the authoritative side and undefined on the mirror. All three are now clauses of one signal:true rule, so the record and the reaction agree on every spelling. A transient probe timeout under a dispatch burst must not feed the cascade breaker as a crash."
    },
    {
      "id": "captured-github-quota-low",
      "input": "[pipeline-start-failure] github-auth-failed: GitHub API quota too low to start pipeline (8/5000 remaining, need ≥200). Resets in ~1 min. (isAuthenticated=true, hasRequiredScopes=false)",
      "expected": "github_quota_low",
      "expected_signal": "github_quota_low",
      "source": "captured",
      "rationale": "Real preflight refusal, and the row that pins a CONJUNCTIVE rule: neither `pipeline-start-failure` nor `github api quota too low` classifies alone — both are required. Dropping either half of the conjunction leaves an environmental, self-clearing condition counted as a pipeline defect."
    },
    {
      "id": "captured-issue-closed",
      "input": "[pipeline-start-failure] issue-closed: Issue #000 is already CLOSED. Pipeline halted before any AI stages. Use forceRerun: true to override.",
      "expected": "issue_closed",
      "expected_signal": "",
      "source": "captured",
      "rationale": "Not a failure — the issue was closed before any AI stage ran. Same conjunctive shape as the quota row above, and pinned for the same reason: the hyphenated `issue-closed` token only classifies together with the `pipeline-start-failure` wrapper."
    },
    {
      "id": "captured-anthropic-weekly-limit",
      "input": "You've hit your weekly limit · resets Jul 24 at 10pm (REDACTED-OWNER/REDACTED-REPO)",
      "expected": "",
      "expected_signal": "",
      "source": "captured",
      "rationale": "A REAL account-level limit envelope that classifies as nothing anywhere. `weekly limit` is a model_unavailable cap-phrase but is gated on the text naming a registry model, and this message names none — deliberate, so an ACCOUNT limit does not trigger #42's model-tier downgrade. The gap is that no quota kind claims it either; recorded as-is and raised in concerns rather than papered over here."
    },
    {
      "id": "captured-anthropic-session-limit",
      "input": "You've hit your limit · resets 10am (REDACTED-OWNER/REDACTED-REPO)",
      "expected": "",
      "expected_signal": "",
      "source": "captured",
      "rationale": "The five-hour sibling of the row above, also real, also unclassified. Kept alongside it because the two differ only by the word `weekly`, which is exactly the kind of near-miss a future matcher will get half-right."
    },
    {
      "id": "network-unavailable",
      "input": "Stage feature-dev cancelled: network unavailable: extended github connectivity loss (5 consecutive probe failures)",
      "expected": "network_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Scheduler stage cancellation with cause ErrNetworkUnavailable (#3296)",
      "rationale": "First on every ladder. The text arrives through context.Cause and contains `cancelled` and `failures`; classified first so it cannot be captured by a generic heuristic, since the correct reaction is to stop dispatching entirely rather than retry one issue."
    },
    {
      "id": "stream-idle-timeout",
      "input": "API Error: Stream idle timeout - partial response received",
      "expected": "stream_idle_timeout",
      "expected_signal": "stream_idle_timeout",
      "source": "synthetic",
      "producer": "Claude CLI result envelope, is_error:true (#3398)",
      "rationale": "Matched before the stall heuristics: the message contains `timeout` and describes an idle stream, so the stall ladder would otherwise claim an API-side cut and blame the agent for the provider's disconnect."
    },
    {
      "id": "api-connection-lost-socket",
      "input": "API Error: The socket connection was closed unexpectedly",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "Claude CLI result envelope carrying the raw transport error (#4002)",
      "rationale": "The canonical transport drop. Distinct from api_overloaded (capacity) but recovered the same way, and it must beat the crash fallback: a seconds-long local network blip is not a code fault and must not pause the fleet."
    },
    {
      "id": "api-connection-lost-connection-closed",
      "input": "API Error: Connection closed mid-response",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "Live wording that motivated #227",
      "rationale": "The wording that fell through in production: `socket connection was closed` needs the `socket` prefix and `connection reset`/`refused` are different verbs, so this matched nothing and halted the fleet on a blip. Gated on `api error` co-occurring so an unrelated stage error mentioning a closed connection is not excused as a transport fault."
    },
    {
      "id": "api-connection-lost-dns",
      "input": "API Error: getaddrinfo EAI_AGAIN while contacting the model endpoint",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "Node DNS failure surfaced through the CLI result envelope (#4002)",
      "rationale": "A DNS-class transport failure. Pinned because the SDK mirror knew only the `connection closed` alternative and none of the error-code alternatives, so every DNS blip classified one way in the record and another way in the published SDK."
    },
    {
      "id": "api-connection-lost-negative-unrelated-econnreset",
      "input": "Integration test failed: ECONNRESET while connecting to the local fixture server",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Negative case for the api_connection_lost `api error` gate",
      "rationale": "The gate exists so that a failing integration test that merely mentions a socket error is not excused as a provider outage. This row is the reason the error-code alternatives are conjunctive with `api error`; delete that conjunction and real test failures start being retried as transient infrastructure."
    },
    {
      "id": "rate-limit-quota-exhausted-underscore",
      "input": "rate_limit_quota_exhausted: five-hour bucket exhausted, overage rejected",
      "expected": "rate_limit_quota_exhausted",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "Underscore kind form, matched for the NotifyComplete defense-in-depth re-classify",
      "rationale": "Go re-classifies a forwarded failureDetail whose kind arrived empty, and by then the kind may appear only in its underscore spelling. Every marker family is matched in bracketed, hyphenated and underscored form for that round trip; this row pins the underscore leg."
    },
    {
      "id": "grok-402-usage-balance-exhausted",
      "input": "API error (status 402 Payment Required): Grok Build usage balance exhausted",
      "expected": "rate_limit_quota_exhausted",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "grok Build CLI's own 402 response text, reaching the classifier as an ordinary stderr/API-error reason via #533's cliFailureText carry — no [rate-limit-quota-exhausted] marker at all, because skillRunner only stamps that bracketed form on the Claude stream-json result-envelope path.",
      "rationale": "Split out of #533's AC2, confirmed unclassified on both layers by direct probe (#560): pre-fix, Classify(...) on this exact text returns \"\" and grokStream.ts's QUOTA_RE does not match \"usage balance exhausted\" either. A billing-exhaustion 402 is the same environmental quota window as the other three producers of this kind (skillRunner's bracketed stamp, the underscore re-classify leg, and the plain hyphenated form) — it must not fall through to subagent_crash, which would count it as a code fault and feed the cascade breaker for a window that clears once the balance resets. Fixed by widening the EXISTING rate-limit-quota-exhausted rule (route (b) from the issue) rather than adding a new kind or only widening grokStream.ts's QUOTA_RE, so the Go CLI path and the generated SDK mirror answer from one authority."
    },
    {
      "id": "model-unavailable-not-found",
      "input": "API Error: model not found: claude-sonnet-5",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic 404 not_found_error naming the model (#42)",
      "rationale": "Routes to the tier-downgrade fallback rather than a retry of the same rejected model. Matched after the explicit quota marker — an explicit stamp beats a heuristic — and before every generic block, because the message also contains `api error` and would otherwise read as a transport drop."
    },
    {
      "id": "model-unavailable-plan-phrase",
      "input": "Your plan does not include claude-opus-4-5: this model is not available on your current plan",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic plan-restriction wording (#42)",
      "rationale": "A plan phrase only classifies when the text also names a registry model — that gate is what keeps ACCOUNT-level limit messages routing to the quota path instead of downgrading a model that was never the problem. This row exercises the positive half; the two captured `You've hit your…limit` rows exercise the negative half."
    },
    {
      "id": "issue-closed-underscore",
      "input": "issue_closed: pickup found the issue already closed",
      "expected": "issue_closed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Underscore kind form for the NotifyComplete re-classify",
      "rationale": "The conjunctive `pipeline-start-failure` + `issue-closed` rule cannot fire on a re-classify that only sees the kind name, so the underscore spelling is matched on its own. Without it a deliberate non-failure is re-derived as a crash on the second pass."
    },
    {
      "id": "architecture-approval-required",
      "input": "ARCHITECTURE APPROVAL REQUIRED: feature-dev halted — a human must approve the storage-engine change before implementation proceeds.",
      "expected": "architecture_approval_required",
      "expected_signal": "architecture_approval_required",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator.verifyArchitectureApproval, ARCHITECTURE_APPROVAL_REQUIRED_MARKER (#4098/#4222)",
      "rationale": "The sentinel is human-readable rather than bracketed because failureComment.ts and the extension's pause rendering already key on that exact text. A deliberate human gate must not be re-dispatched: before it had a kind, each attempt cost a full stage and the second one tripped the lifetime cap on a gate only a human can open."
    },
    {
      "id": "github-quota-low-marker",
      "input": "[github-quota-low] GitHub API quota too low — pipeline deferred before AI stages (transient; resetInSec=180).",
      "expected": "github_quota_low",
      "expected_signal": "github_quota_low",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator quota preflight, the error.message form (#3896)",
      "rationale": "The SECOND wording for one condition. The captured row for this kind carries the stderr phrasing (`[pipeline-start-failure] github-auth-failed: GitHub API quota too low …`), which the table claims through the `pipeline-start-failure` + `github api quota too low` conjunction; this row carries the bare token, which the first clause claims. Both are signal:true and both now project the same kind on every side — before #306 the extension's ladder keyed on the token alone and missed the captured shape entirely. Two producers, one condition, two spellings: exactly the shape that let a matcher rot unnoticed."
    },
    {
      "id": "github-network-outage",
      "input": "[pipeline-start-failure] github-network-outage: api.github.com unreachable at preflight (ENOTFOUND)",
      "expected": "github_network_outage",
      "expected_signal": "github_network_outage",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator.preCheckAuth (#4002)",
      "rationale": "The connectivity sibling of github_quota_low: environmental, self-clearing, and routed to a short GLOBAL cooldown rather than a per-issue one, because if GitHub is unreachable no issue will fare better. Note it does NOT read as api_connection_lost — that gate needs `api error`, and this text says `api.github.com`."
    },
    {
      "id": "pr-merge-unmerged-marker",
      "input": "[pr-merge-unmerged:ci_failures] pr-merge session completed but the PR is still open",
      "expected": "pr_merge_unmerged",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator.diagnosePrMergeBlocker (#3691)",
      "rationale": "The stamp carries the blocker classification inside the marker, so the prefix is matched without its closing bracket. Ordered before the budget and stall heuristics so the rich diagnostic survives instead of being flattened into a generic kind."
    },
    {
      "id": "order-pr-merge-unmerged-beats-premature-turn-end",
      "input": "premature turn end: pr-merge reported success but PR #000 is not merged (state: OPEN). blocked by required checks",
      "expected": "pr_merge_unmerged",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Post-merge verification gate, whose wording carries no marker (#3691)",
      "rationale": "ORDERING ROW. The post-merge verification gate is a second route to this state and phrases it without the stamp, while the scheduler wraps every no-op gate reason in `premature turn end:`. Both matchers see this string; pr_merge_unmerged must win, or each CI-blocked merge increments the lifetime failure counter and re-dispatches into the same red check until the cap trips the whole scheduler. Go wins it by ordering, the SDK by an explicit exclusion — different mechanisms, same answer, which is exactly why it needs a row."
    },
    {
      "id": "order-containment-breach-beats-premature-turn-end",
      "input": "[stage:worktree-containment] premature turn end: stage exited 0 with a clean worktree",
      "expected": "containment_breach",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Composite of the containment marker and the scheduler's no-op wrapper",
      "rationale": "ORDERING ROW. A breaching stage usually ALSO exits 0 with nothing in its own tree, so both matchers fire. Containment wins because it names the actual problem — work landed in a repository the run does not own — whereas premature_turn_end would tell the operator the agent did nothing."
    },
    {
      "id": "not-pipeline-actionable-marker",
      "input": "premature turn end: stage exited 0 with no state change (gate no-op): [not-pipeline-actionable] feature-dev declared the issue human-only: the deliverable is counsel sign-off, which no pipeline stage can produce",
      "expected": "not_pipeline_actionable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-dev's NOT_PIPELINE_ACTIONABLE declaration, wrapped by the scheduler's no-op wrapper (#1241)",
      "rationale": "The specimen run's shape. A stage that correctly refuses an unimplementable issue leaves an empty tree, so the gate fires its no-op verdict and the scheduler wraps it — the declaration has to survive that wrapper, which is what the marker is for."
    },
    {
      "id": "order-not-pipeline-actionable-beats-dev-produced-no-changes",
      "input": "premature turn end: stage exited 0 with no state change (gate no-op): [not-pipeline-actionable] the issue asks for a legal review; [dev-produced-no-changes] dev reported file changes but produced none in the stage workspace",
      "expected": "not_pipeline_actionable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Composite of the stage's declaration and the gate's empty-tree verdict",
      "rationale": "ORDERING ROW, and the one this change turns on. Both markers describe the SAME empty workspace and both rules match; they differ in who is at fault. dev_produced_no_changes is agent-class — it increments the lifetime failure cap and halts the repository — while the declaration says the stage did the right thing and the issue was misfiled. The stage's own structured declaration outranks the gate's inference from an empty tree, so this rule sits above it."
    },
    {
      "id": "order-dev-produced-no-changes-beats-premature-turn-end",
      "input": "premature turn end: [dev-produced-no-changes] feature-dev gate found a clean tree and a branch level with base",
      "expected": "dev_produced_no_changes",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-dev stage gate wrapped by the scheduler's no-op wrapper (#202)",
      "rationale": "ORDERING ROW. dev_produced_no_changes is a NARROWER premature turn end and is always delivered inside that wrapper, so if the generic block ran first this kind could never appear on a text-classified path — only the gate path would ever report it, and the two paths would disagree about the same run."
    },
    {
      "id": "order-dev-handoff-missing-beats-premature-turn-end",
      "input": "premature turn end: [dev-handoff-missing] dev context absent but git reports changed files in the worktree",
      "expected": "dev_handoff_missing",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-dev stage gate wrapped by the scheduler's no-op wrapper (#223)",
      "rationale": "ORDERING ROW, and the inverse of the one above. The distinction is not cosmetic: this kind means there IS work on disk worth preserving before the next worktree sweep, and the generic kind means there is not."
    },
    {
      "id": "order-git-transport-auth-beats-premature-turn-end",
      "input": "error: failed to push some refs: invalid auth method — then stage issue-pickup exited 0 but did not write expected output context: /w/.nightgauge/context/issue-pickup.json",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The post-condition site's composed reason (scheduler.go: `<first cause> — then <post-condition symptom>`, #878)",
      "rationale": "THE #878 ROW. The composed reason carries BOTH the cause and the symptom, and the symptom phrase is a clause of premature-turn-end. Whichever rule sits earlier wins the whole string, so ordering is the entire fix: a credential-less push booked as a turn-end tells the retro path the agent gave up when in fact the machine had no credential, and routes recovery at the agent instead of at the environment."
    },
    {
      "id": "git-transport-auth-ssh-publickey",
      "input": "git@example.org: Permission denied (publickey).\nfatal: Could not read from remote repository.",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "ssh's own refusal, as git surfaces it when PushBranch shells out to the git CLI against an SSH remote (#880)",
      "rationale": "The default remote shape for anyone who cloned over SSH, and the reason the bare phrase `permission denied` is NOT a term: this row matches on `(publickey`, which no filesystem EACCES carries. Pair it with permission-denied-negative-eacces to see the boundary — one is a key the remote will not take, the other is a directory the process cannot write, and they have opposite remedies."
    },
    {
      "id": "git-transport-auth-https-no-credential-helper",
      "input": "fatal: could not read Username for 'https://example.org': terminal prompts disabled",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "git CLI over an HTTPS remote with no credential helper and GIT_TERMINAL_PROMPT=0",
      "rationale": "The headless half of the same fault: there is no credential to offer and nobody to ask for one. Without this rule it read as an ordinary non-zero exit and fell to subagent_crash, which counts toward the lifetime failure cap for a machine that was simply never given a token."
    },
    {
      "id": "git-transport-auth-https-password",
      "input": "fatal: could not read Password for 'https://bot@example.org': No such device or address",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "git CLI over HTTPS when the username resolved from the URL but no password source exists",
      "rationale": "The password half of git's credential prompt, spelled separately because git emits one message or the other depending on which half of the pair it already has. Pinning both keeps a future edit from collapsing them into a single `could not read` term, which would also claim `could not read from remote repository` — the SSH row's second line, and a different failure."
    },
    {
      "id": "git-transport-auth-invalid-username-or-password",
      "input": "remote: Invalid username or password.\nfatal: Authentication failed for 'https://example.org/acme/widget.git/'",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A forge's HTTPS push rejection as git relays it (`remote:` prefix plus git's own fatal)",
      "rationale": "Pins two clauses at once on the string a real HTTPS push rejection produces, and pins the NARROWED form `authentication failed for`: git always names the remote it failed against, so the `for` is free, and requiring it keeps an adapter CLI's bare `authentication failed` — a MODEL credential, a different remedy — out of a git-transport kind."
    },
    {
      "id": "git-transport-auth-negative-bare-authentication-failed",
      "input": "API Error: authentication failed while opening the model stream",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Negative case protecting the narrowing of the `authentication failed for` term",
      "rationale": "THE TRAP ROW for this rule. A vendor CLI's own bare `authentication failed` is the MODEL adapter refusing, not a git remote: its remedy is `claude login`, not an SSH key or a token in a credential helper. This rule sits above sixteen others and matches the whole joined string, so widening the term to the bare phrase would let it claim adapter and forge failures alike — the exact single-ladder mistake #306 exists to prevent."
    },
    {
      "id": "git-transport-auth-ssh-agent-exhausted",
      "input": "ssh: unable to authenticate, attempted methods [none publickey], no supported methods remain",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "golang.org/x/crypto/ssh, surfaced through go-git's SSH transport",
      "rationale": "The go-git-side spelling of the SSH refusal, distinct from the CLI's `Permission denied (publickey)` because the two transports print nothing in common. #593's workaround was present on one write path and absent on another; the classification has to cover both producers or the kind depends on which call site happened to run."
    },
    {
      "id": "git-transport-auth-forge-bad-credentials",
      "input": "gh: Bad credentials (HTTP 401)",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The GitHub API's 401 body as `gh` prints it",
      "rationale": "The forge-API sibling of the transport failures above: the token is present but rejected, so every board read, PR create and merge in the run will fail the same way. Matched on the API's own words rather than on `401`, because the bare code appears in issue numbers and temp paths — the same trap that made orchestrator.permissionPhrases spell its codes out."
    },
    {
      "id": "git-transport-auth-go-git-authentication-required",
      "input": "epic branch auto-create: push epic branch epic/900-example: push branch epic/900-example: authentication required",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "go-git's transport.ErrAuthenticationRequired, as the scheduler's epic-branch auto-create surfaces it when the remote wants credentials and none were supplied",
      "rationale": "The spelling permissionPhrases lists and FAILURE_TAXONOMY names as a git-transport form, and the one this rule shipped WITHOUT: the escalation gate declined to escalate this exact text while the record still booked validation_error, so the run simultaneously concluded 'permission failure, no model can fix it' and 'the stage wrote a malformed context'. Unambiguous at whole-string scope — nothing but a transport asks for authentication in these words — which is why it is in and the bare 403 forms are not. The prefix is the observed #878 shape: the failing push is the SCHEDULER's, appended to the stage's evidence by AppendStageOutputTail because it never passes through the subagent's output at all."
    },
    {
      "id": "git-transport-auth-forge-http-401",
      "input": "forge api: HTTP 401: Requires authentication (https://api.example.org/graphql)",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A forge REST/GraphQL 401 as the CLI prints the status line, with no `Bad credentials` body",
      "rationale": "Pins `http 401` on its own: the sibling row git-transport-auth-forge-bad-credentials also contains HTTP 401, so without this row the clause could be deleted and the suite would stay green on the other row's `bad credentials` term. 401 is the ONE HTTP code that means exactly 'your credentials were refused' — unlike 403, which is also GitHub's throttle code and is therefore excluded (boundary-negative-left-edge-session-limit). Written form only, never the bare code: `#401` is an issue number and `/tmp/Test401.../` is a path."
    },
    {
      "id": "git-transport-auth-forge-401-unauthorized",
      "input": "remote rejected the deploy token: 401 Unauthorized",
      "expected": "git_transport_auth_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The other written form of the same status, as an HTTP transport or a forge client wraps it",
      "rationale": "The second written 401 form permissionPhrases carries, pinned separately because the two spellings share no substring — `http 401` does not appear in `401 Unauthorized` and deleting either clause has to go red on its own row. Bare `unauthorized` is still absent: it appears in unrelated refusal prose, and this rule matches the whole joined string above sixteen other rules."
    },
    {
      "id": "premature-turn-end-gate",
      "input": "premature turn end: feature-planning gate reported no state change",
      "expected": "premature_turn_end",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Gate hook stamping gates.KindNoOp (#74)",
      "rationale": "The generic form, once the three narrower kinds above have had their chance. Pinned to hold the boundary between them: this row must stay generic while those stay specific, or the ordering above is untested."
    },
    {
      "id": "premature-turn-end-no-output-context",
      "input": "Stage feature-planning exited 0 but did not write expected output context",
      "expected": "premature_turn_end",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "validateStageOutput (#2870)",
      "rationale": "Its only producer runs exclusively on exit-0 paths, so the phrase always means the agent ended its turn without writing state. It previously bucketed into validation_error, which sent an operator hunting a schema problem that did not exist."
    },
    {
      "id": "worktree-uncommitted",
      "input": "worktree_uncommitted: recovered 3 uncommitted files into a recovery commit",
      "expected": "worktree_uncommitted",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Scheduler auto-recovery marker (#3542)",
      "rationale": "A failure whose WORK SURVIVED — the distinction the operator needs before deciding whether to re-run. Matched early so the generic exit/stall heuristics do not shadow it."
    },
    {
      "id": "worktree-uncommitted-stop-hook-alias",
      "input": "stop_hook_uncommitted: stop hook found uncommitted changes at stage exit",
      "expected": "worktree_uncommitted",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Stop-hook kill marker alias (#3542)",
      "rationale": "A second marker deliberately mapped onto one kind. Pinned so a future reader does not 'tidy' the alias away and silently create a fourth unclassified failure shape."
    },
    {
      "id": "order-budget-ceiling-beats-budget-exceeded",
      "input": "PIPELINE BUDGET CEILING exceeded — pipeline budget ceiling of $30.00 reached; budget exceeded for this run",
      "expected": "budget_ceiling_hit",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Composite of the USD ceiling wording and the token-budget enforcer wording",
      "rationale": "ORDERING ROW, and the exact drift #306 found: the SDK's budget_ceiling_hit block carried a comment stating it must be matched before budget_exceeded while physically sitting after it. The run-level USD ceiling and the per-run token budget are different limits with different operator responses, and the ceiling is the larger, more specific claim."
    },
    {
      "id": "budget-ceiling-underscore",
      "input": "budget_ceiling_hit: run stopped at the configured USD ceiling",
      "expected": "budget_ceiling_hit",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Underscore kind form for the NotifyComplete re-classify",
      "rationale": "The re-classify leg for the ceiling kind. Without it the round trip degrades a ceiling stop into the generic budget kind, which is the same silent downgrade the ordering row above pins from the other direction."
    },
    {
      "id": "runaway-progress-exit-signal-source-dead-branch",
      "input": "exitSignalSource=runaway-progress; stage terminated by the progress monitor",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Exit-signal telemetry path (#3783) — failure_handler.go's second runaway-progress alternative",
      "rationale": "RECORDS A DEFECT, does not fix it. Go intends a second route to runaway_progress via the conjunction of `exitSignalSource` and `runaway-progress`, but matching is done against the LOWERCASED text while that literal carries capitals, so the clause cannot fire for ANY input — TestDeadTermsAreUnreachable proves it rather than asserting it. The corpus pins what actually ships (unclassified, falling through to the caller's crash default) rather than what the source implies; lowercasing the literal would change the authoritative classifier's live routing, which is a taxonomy decision, not a parity fix, and is raised in this PR's concerns. The clause is now in the table, so BOTH languages carry the dead branch and its declaration in `dead_terms` — before #306 the SDK silently omitted it, which is the one-sided difference this change exists to remove."
    },
    {
      "id": "stall-kill-runaway-ceiling",
      "input": "[runaway-ceiling-exceeded] Stage feature-dev terminated: runaway cost ceiling exceeded",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Runaway-ceiling kill (#3508)",
      "rationale": "Deliberately mapped onto stall_kill rather than a budget kind: recovery is identical to a stall (backoff, no lifetime-cap increment) because the ceiling is a safety rail, not the operator's spending decision. Matched before the cost-cap block so the two ceilings do not swap recoveries."
    },
    {
      "id": "order-cost-cap-beats-stall",
      "input": "[cost-cap-exceeded] Stage feature-dev terminated after it exceeded stall idle threshold",
      "expected": "budget_exceeded",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Composite of the cost-cap marker and the stall watchdog wording",
      "rationale": "ORDERING ROW. The cost-cap kill IS delivered as an idle SIGTERM, so its text can legitimately carry both. Budget must win: a run that deliberately spent its cap should not be retried as a transient stall and spend it again."
    },
    {
      "id": "stall-kill-stage-no-output-timeout",
      "input": "[stage-no-output-timeout] Stage feature-dev produced no session output within 10m 0s",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator first-output watchdog (#252)",
      "rationale": "A zombie run, classified transient-stall because retry with backoff is the correct recovery and it must not count against the lifetime cap. This is now the zombie-run row's ONLY clause — #470 retired the sibling clause after #427 deleted its only producer."
    },
    {
      "id": "stall-kill-marker-only",
      "input": "[stall-killed] pr-create terminated",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "SkillRunner.ts stall-kill errorText (#3207)",
      "rationale": "The bare marker with no descriptive tail — the shortest real form the stall path emits. It is the row that proves membership rather than prose: strip the marker literal from a ladder and only this row goes red."
    },
    {
      "id": "budget-exceeded-stage-enforcer",
      "input": "stage_budget_exceeded: feature-dev consumed its entire stage token budget",
      "expected": "budget_exceeded",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "budget_enforcer.go BudgetDecision.Reason",
      "rationale": "The Go-side enforcer reason, as opposed to the extension's cost-cap marker. Both mean the run stopped on a limit the operator set; pinning both keeps the two producers from drifting into different kinds."
    },
    {
      "id": "validation-error-schema",
      "input": "Stage output failed schema validation: required field 'summary' missing",
      "expected": "validation_error",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Stage output schema check",
      "rationale": "A genuine machine-readable-output failure, distinct from feature-validate's quality gate. Kept narrow — the `did not write expected output context` phrase deliberately moved OUT of this kind to premature_turn_end, and this row holds what remains."
    },
    {
      "id": "validation-error-not-valid-json",
      "input": "Stage handoff was not valid json",
      "expected": "validation_error",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Stage output parse failure",
      "rationale": "The parse-failure leg of the same kind. Pinned separately because it and the schema leg are matched by different literals, and a ladder that keeps only one of them fails silently on the other."
    },
    {
      "id": "adapter-auth-failed-bracket",
      "input": "[adapter-auth-failed] Auth pre-flight failed — probe timed out after retry",
      "expected": "adapter_auth_failed",
      "expected_signal": "adapter_auth_failed",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator auth pre-flight (#312)",
      "rationale": "The bracketed spelling, alongside the captured bare one. Both must classify: a transient probe timeout under a concurrent dispatch burst is not a process death, and three burst false-negatives fed to the cascade breaker as crashes pause the whole fleet."
    },
    {
      "id": "grok-not-signed-in",
      "input": "Error: Not signed in. To authenticate without a browser, run: grok login --device-code. Alternatively, set the XAI_API_KEY environment variable or run grok login on a machine with a browser.",
      "expected": "adapter_auth_failed",
      "expected_signal": "adapter_auth_failed",
      "source": "synthetic",
      "producer": "grok Build CLI's own stderr when no credentials are present (#591), reaching the classifier as an ordinary stderr reason via #533's cliFailureText carry — no [adapter-auth-failed] marker at all, because the pipeline-start probe never runs for a CLI that fails this fast.",
      "rationale": "Pinned from the #528 live probe (run 2026-08-15 against #590): grok credentials absent, the CLI fails in seconds, and this exact sentence lands on stderr verbatim. Pre-#591 it satisfied no rule and fell all the way through to subagent_crash — the exact misclassification #528 forbids — and the retry engine then escalated haiku to sonnet on an error a stronger model cannot fix. Marked synthetic (not captured) because it is transcribed from the issue body rather than replayed through scripts/capture-terminal-kind-fixture.sh against real telemetry logs; it widens the EXISTING adapter-auth-failed rule rather than adding a new kind, because this is the same auth-shaped taxonomy bucket as the pipeline-start probe failure, just surfaced mid-dispatch instead of at preflight."
    },
    {
      "id": "no-changes-produced",
      "input": "[no-changes-produced] pr-create confirmed the feature branch has zero commits ahead of base",
      "expected": "no_changes_produced",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "pr-create deterministic fallback (#317)",
      "rationale": "There is genuinely nothing to open a PR for — a dispatched human-only issue, not a defect. Deliberately NOT matched on the bare phrase `no commits ahead of`, which also appears in feature-validate's unrelated lost-implementation check and must keep its organic classification."
    },
    {
      "id": "validation-failed",
      "input": "[validation-failed] feature-validate wrote validation_status=failed and left the work uncommitted for retry",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate quality gate (#326)",
      "rationale": "An HONEST failure: the pipeline's own gate caught a real implementation problem. It carries full organic weight, unlike the transient kinds around it — misfiling it as a crash would both excuse the code and pause the queue."
    },
    {
      "id": "validation-inconclusive",
      "input": "[validation-inconclusive] feature-validate unit tier ran but executed zero tests",
      "expected": "validation_inconclusive",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate zero-test detection (#221)",
      "rationale": "The opposite claim to validation_failed: the gate learned nothing, usually from an environmental misconfiguration. Keeping the two apart is what stops an unrun suite from being recorded as a passing one or as a code defect."
    },
    {
      "id": "branch-forked-marker",
      "input": "[branch-forked] pre-stage fork pre-flight: the run's branch has diverged from its remote",
      "expected": "branch_forked",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Go scheduler fork pre-flight (#163)",
      "rationale": "Detected before the stage spends a token. No retry clears a fork, so classifying it as a crash produces an infinite loop of identical failures — which is precisely what happened before the kind existed."
    },
    {
      "id": "branch-forked-push-rejected",
      "input": "PUSH REJECTED: non-fast-forward — updates were rejected because the remote contains work you do not have locally",
      "expected": "branch_forked",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Skill push failure wording (#163)",
      "rationale": "The second entry point to one kind, recognised by git's own phrase. `non-fast-forward` is sufficient on its own because git emits it for exactly this condition and nothing else, and it must beat the crash fallback whose `exit ` substring would otherwise read a push rejection as a process death."
    },
    {
      "id": "branch-forked-negative-plain-push-rejection",
      "input": "Stage feature-validate: push rejected by the pre-receive hook (branch protection)",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Negative case for the `push rejected` + `fetch first` conjunction",
      "rationale": "`push rejected` alone is NOT a fork — a protected-branch refusal has a different remedy entirely. This row is why the phrase is conjunctive with `fetch first`, and it goes red the moment someone relaxes that."
    },
    {
      "id": "commit-orphaned",
      "input": "[commit-orphaned] HEAD is on temp-pre-push-2 instead of the expected feature branch and self-heal could not recover it",
      "expected": "commit_orphaned",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate branch-identity guard (#266)",
      "rationale": "Valid work exists but sits where nobody will look for it, after a SIGKILL bypassed the pre-push restore-defer. Unrecoverable by retry, so it must not be classified as anything retryable."
    },
    {
      "id": "permission-denied-marker",
      "input": "[permission-denied] the harness refused a foreground sleep wait loop",
      "expected": "permission_denied",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Harness tool-call denial (#289)",
      "rationale": "A denial is the harness saying 'not that way', not a defect: the stage had turns left and could have chosen another approach. It routes to a short backoff with no cascade feed — where the crash fallback would have paused the fleet over one rejected tool call. The SDK mirror had NO permission_denied matcher at all."
    },
    {
      "id": "permission-denied-tool-use-result",
      "input": "Stage feature-dev result envelope is_error=true, tool_use_result: User rejected tool use",
      "expected": "permission_denied",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Claude CLI result envelope for a denied tool call (#289)",
      "rationale": "The shape the CLI actually reports, matched both by the literal sentence and by the conjunction of `tool_use_result` and `rejected`. Two spellings, one kind, because the envelope wording is not ours to stabilise."
    },
    {
      "id": "permission-denied-negative-eacces",
      "input": "EACCES: permission denied while writing the stage context file",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Negative case protecting the permission_denied matcher's shape",
      "rationale": "THE TRAP ROW. A filesystem EACCES is an infrastructure fault, not a harness denial, and the two have opposite recoveries. This is why the matcher keys on `[permission-denied]`, `permission_denied`, `user rejected tool use` and the tool_use_result conjunction — and never on the bare phrase `permission denied`. Implementing the block with /permission denied/ would classify every EACCES as a harness denial on one side only, creating a NEW divergence while claiming to close one."
    },
    {
      "id": "subagent-crash-exit-code",
      "input": "exit 1: unexpected end of input while reading the session stream",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "scheduler.SetStageError's `exit N: …` prefix",
      "rationale": "The fallback, and the reason ordering matters everywhere above: SetStageError prefixes almost every stage error with `exit N: `, so the crash matcher can claim any string that reached it. Every specific kind is therefore matched BEFORE this one — a rule appended after it is dead code."
    },
    {
      "id": "subagent-crash-signal",
      "input": "Stage feature-dev killed by signal SIGKILL",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Process supervision",
      "rationale": "The genuine process-death leg of the fallback — a real crash, correctly weighted and correctly fed to the cascade breaker. Pinned so the fallback keeps meaning something after everything else was taken out of it."
    },
    {
      "id": "unknown-operator-stop",
      "input": "Cancelled by user",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Stop All / abortAll cancellation text",
      "rationale": "THE DEFAULT CASE, and a deliberate one. An operator Stop matches nothing on any ladder and books as a generic failure on BOTH execution paths — the terminal_behaviors.json force-clear accounting names this and declines to fix only the wedged variant, since marking one Stop path and not the other would make them disagree about what a Stop costs."
    },
    {
      "id": "unknown-empty-error-text",
      "input": "",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Empty errorText guard",
      "rationale": "Empty input short-circuits before any matcher runs. Trivial, and pinned anyway because it is the one input where an early-return refactor could start returning the fallback kind and quietly attribute a crash to every run with no error text."
    },
    {
      "id": "order-model-unavailable-beats-quota-wording",
      "input": "Claude Opus 4.5 usage limit reached; resets at 5pm",
      "expected": "model_unavailable",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "the Anthropic CLI's plain-text limit line, surfaced by SkillRunner.extractTailError",
      "rationale": "Both the model-unavailable cap wording and the quota extension describe this text, and they answer differently ON PURPOSE — this is the one declared record-vs-reaction divergence. The RECORD says model_unavailable: a named model plus a cap phrase is a plan restriction, and the rule sits above the generic rungs so it wins. The REACTION says rate_limit_quota_exhausted, because signal_extensions restores the #3792 defense-in-depth the extension ladder carried before #306, and the model-unavailable rule is not in the signal subset so nothing of the record's is being overruled. Delete the extension and this row's expected_signal goes red."
    },
    {
      "id": "order-api-overloaded-beats-quota-marker",
      "input": "API Error: Overloaded, and [rate-limit-quota-exhausted] resetsAt=0",
      "expected": "api_overloaded",
      "expected_signal": "api_overloaded",
      "source": "synthetic",
      "producer": "Composite of the 529 envelope and the quota stall marker",
      "rationale": "ORDERING ROW, and the second record-vs-reaction split #306 closed. The table tests `overloaded` before the explicit quota marker, so an incidental capacity word beats a deliberate stamp; the extension's old ladder tested them the other way round and Go used its answer verbatim, so a five-minute retry and a cooldown-until-reset were chosen for the same failure by the same run. Both sides now read this table, so the order is whatever it says here — and it is arguably the wrong way round. Changing it is a live routing change to the authoritative classifier, which is a taxonomy decision, not a parity fix."
    },
    {
      "id": "order-adapter-auth-failed-beats-subagent-crash",
      "input": "exit 1: [adapter-auth-failed] claude-headless=auth probe timed out after 5s",
      "expected": "adapter_auth_failed",
      "expected_signal": "adapter_auth_failed",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator.preCheckAuth's [adapter-auth-failed] marker, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, a transient auth-probe starvation would be recorded as a process death, fed to the cascade breaker and counted against the lifetime failure cap instead of taking the short-backoff retry."
    },
    {
      "id": "order-no-changes-produced-beats-subagent-crash",
      "input": "exit 1: [no-changes-produced] pr-create confirmed the feature branch has zero commits ahead of base",
      "expected": "no_changes_produced",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "pr-create's deterministic create fallback, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, a dispatch that produced nothing to open a PR for would be recorded as a crash, hiding the planning/scope defect it actually is."
    },
    {
      "id": "order-validation-failed-beats-subagent-crash",
      "input": "exit 1: [validation-failed] feature-validate wrote validation_status=failed and left the work uncommitted for retry",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate's quality-gate exit, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, an honest quality-gate failure — the pipeline's own gate working — would be recorded as a subagent process death."
    },
    {
      "id": "order-validation-inconclusive-beats-subagent-crash",
      "input": "exit 1: [validation-inconclusive] feature-validate unit tier ran but executed zero tests",
      "expected": "validation_inconclusive",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate's zero-test guard, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, a gate that learned nothing would be recorded as a crash, and the environmental misconfiguration behind it would never be triaged."
    },
    {
      "id": "order-branch-forked-beats-subagent-crash",
      "input": "exit 1: PUSH REJECTED: non-fast-forward — updates were rejected because the remote contains work you do not have locally",
      "expected": "branch_forked",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the skills' push-rejection sentence surfacing from a failed `git push`, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, every retry of the same unrecoverable fork would look like a fresh crash — the exact misreading #163 was filed for."
    },
    {
      "id": "order-commit-orphaned-beats-subagent-crash",
      "input": "exit 1: [commit-orphaned] HEAD is on temp-pre-push-2 instead of the expected feature branch and self-heal could not recover it",
      "expected": "commit_orphaned",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate's Step 5.1 branch-identity guard, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, stranded but valid work would be recorded as a crash and swept, instead of being reported as unrecoverable-by-retry."
    },
    {
      "id": "order-permission-denied-beats-subagent-crash",
      "input": "exit 1: [permission-denied] the harness refused a foreground sleep wait loop",
      "expected": "permission_denied",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the harness denial surfaced through the adapter result envelope, wrapped by internal/orchestrator/scheduler.go, which records a failed stage as `exit <N>: <text>` via runtime.SetStageError",
      "rationale": "ORDERING. failure_handler.go says this block MUST be matched before the generic subagent_crash fallback, whose `exit ` substring would otherwise claim the text. Nothing pinned that until this row: the only corpus input containing `exit ` was the crash row itself, so moving this block below the fallback left all three suites green. If it regressed, one rejected tool call becomes a permanently killed run and a fleet-wide pause — #289's incident verbatim."
    },
    {
      "id": "api-connection-lost-socket-hang-up",
      "input": "API Error: socket hang up",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "the Node transport error surfaced verbatim by the Claude CLI result envelope",
      "rationale": "`socket hang up` is a separate alternative from `socket connection was closed` — the words share no substring, so a matcher written for one leaves the other unclassified, which is how the SDK mirror lost patterns before #306. It is also one of twelve clauses on a signal:true rule, so the kind it produces is forwarded to Go and used VERBATIM: the whole rule has to be right on both sides, not just readable on one."
    },
    {
      "id": "api-connection-lost-fetch-failed",
      "input": "API Error: fetch failed (cause: connect ECONNREFUSED)",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "undici's `TypeError: fetch failed` with its connect cause, relayed by the adapter",
      "rationale": "pins two alternatives at once (`fetch failed`, `econnrefused`) AND the `api error` conjunction that gates them: without the gate an integration test that merely mentions ECONNREFUSED would be bucketed as an Anthropic transport drop and retried instead of failed."
    },
    {
      "id": "api-connection-lost-connection-reset",
      "input": "API Error: connection reset by peer while streaming the response",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "the raw transport error relayed by the Claude CLI result envelope",
      "rationale": "`connection reset` is a distinct verb from `connection closed` and from the `econnreset` error code — #227 landed because a wording nobody had pinned fell through to the crash fallback and halted the fleet on a blip."
    },
    {
      "id": "api-connection-lost-connection-refused",
      "input": "API Error: connection refused while opening the stream to the model endpoint",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "the raw transport error relayed by the Claude CLI result envelope",
      "rationale": "the prose sibling of `econnrefused`: the captured shape `API Error: Unable to connect to API (ConnectionRefused)` proves the CLI does phrase this failure in prose, and that shape (no space) does NOT match this literal — so nothing else exercises it."
    },
    {
      "id": "architecture-approval-required-bracket-marker",
      "input": "[architecture-approval-required] feature-planning halted — a human must approve the storage-engine change",
      "expected": "architecture_approval_required",
      "expected_signal": "architecture_approval_required",
      "source": "synthetic",
      "producer": "the bracketed form both text classifiers match alongside the human-readable ARCHITECTURE APPROVAL REQUIRED sentinel",
      "rationale": "The human-readable sentinel is load-bearing in failureComment.ts, so the bracketed form exists precisely so a future emitter change cannot silently re-route a human gate into subagent_crash — which cost ~$5 per re-dispatch and tripped the scheduler on the second attempt (#4098/#4222). The rule is signal:true and all three spellings belong to it, so this row's reaction side is the kind itself: before #306 the extension keyed only on the sentinel and this bracketed form deferred to Go, which is the asymmetry the table removes."
    },
    {
      "id": "stall-kill-stalled-and-killed",
      "input": "Stage feature-dev stalled and killed after 30m 0s without output",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the auto-mode (CLI) stall wording, as opposed to the IPC stage runner's `exceeded stall idle threshold`",
      "rationale": "one of three real phrasings for the single most common failure this machine produces; each differs by a word, so a matcher written for one leaves the others unclassified — which is exactly the drift #306 found on the SDK side."
    },
    {
      "id": "stall-kill-stage-hard-cap",
      "input": "Stage feature-dev exceeded stage_hard_cap (45m 0s) — forcibly terminating process.",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "PipelineBridge's stage hard-cap kill, reported in the IPC stage result errorText",
      "rationale": "the canonical hard-cap marker, and unexercised before this row: deleting it from either ladder left every corpus suite green while Go said stall_kill and the SDK said nothing for the same kill."
    },
    {
      "id": "stall-kill-hard-cap-wording",
      "input": "Stage feature-validate terminated: hard cap of 45m 0s reached",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the auto-mode hard-cap wording (a space, not the `stage_hard_cap` identifier)",
      "rationale": "`hard cap` and `exceeded stage_hard_cap` look like the same rule but share no substring — the identifier spells it `hard_cap` — so they are two independently deletable alternatives and need two rows."
    },
    {
      "id": "budget-exceeded-pipeline-enforcer",
      "input": "pipeline_budget_exceeded: the run consumed its entire pipeline token budget",
      "expected": "budget_exceeded",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "budget_enforcer.go's BudgetDecision.Reason for the pipeline-level token budget",
      "rationale": "the sibling of stage_budget_exceeded and the one that was unpinned: it must land on budget_exceeded rather than the USD budget_ceiling_hit above it, because the two have different recoveries and the ceiling block is deliberately matched first."
    },
    {
      "id": "validation-error-invalid-json",
      "input": "Stage handoff could not be parsed: invalid json at position 412",
      "expected": "validation_error",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the context-file parse failure reported by the stage output validator",
      "rationale": "`invalid json`, `not valid json` and `unparseable json` are three separately deletable spellings of the same fault and only one of them had a row; a parse failure that stops matching falls through to the crash fallback and gets a retry policy meant for process deaths."
    },
    {
      "id": "validation-error-unparseable-json",
      "input": "Stage context is unparseable json — the writer was interrupted mid-flush",
      "expected": "validation_error",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the context-file parse failure reported by the stage output validator",
      "rationale": "The third spelling (see validation-error-invalid-json). All three are alternatives of one clause set, and pinning each separately is what stops a partial deletion: the derived stress set proves every clause is reachable, and these rows prove each spelling means the same thing to a reader. The kind is not in the signal subset, so the reaction defers to Go."
    },
    {
      "id": "validation-error-missing-prerequisite",
      "input": "Stage feature-validate refused to start: missing prerequisite context from feature-dev",
      "expected": "validation_error",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the stage pre-condition check that refuses to run a stage whose input context is absent",
      "rationale": "a missing prerequisite is a pipeline-plumbing fault, not an implementation failure: misbucketing it into the crash fallback charges an organic failure to code that was never run."
    },
    {
      "id": "branch-forked-push-rejected-fetch-first",
      "input": "PUSH REJECTED: updates were rejected because the tip of your current branch is behind its remote counterpart; fetch first before pushing again",
      "expected": "branch_forked",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "git's own rejection text surfaced by the skills' push step",
      "rationale": "this is the ONLY row that exercises the `push rejected` AND `fetch first` conjunction — the other fork rows match on the bare `non-fast-forward` phrase, and the negative row proves only that `push rejected` alone must miss. Deleting the conjunction from either side was invisible before this row."
    },
    {
      "id": "subagent-crash-explicit-phrase",
      "input": "subagent crash: the stage process died without writing a result envelope",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the explicit crash wording used by the auto-mode execution manager",
      "rationale": "the fallback block has three alternatives and only `exit ` and `killed by signal` had rows. It is the block every unmatched failure lands in, so its own literals must be pinned too — a deletion here silently converts real crashes into the unknown/default case."
    },
    {
      "id": "model-unavailable-not-found-error",
      "input": "API Error: 404 not_found_error — model: claude-opus-4-5",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic's 404 error body when the requested model is not resolvable",
      "rationale": "the canonical 404 shape for #42's tier-downgrade fallback, and the one gated on a co-occurring `model` mention so an unrelated not_found_error cannot trigger a model downgrade. Neither half was exercised before this row."
    },
    {
      "id": "model-unavailable-invalid-model",
      "input": "API Error: invalid model: claude-sonnet-9-preview",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic's rejection of an unrecognised model identifier",
      "rationale": "`invalid model` and `unknown model` are ungated (the phrase itself names a model), unlike the plan/cap phrases below — so they are the two literals whose deletion would silently disable the downgrade for the most direct rejection shape there is."
    },
    {
      "id": "model-unavailable-unknown-model",
      "input": "API Error: unknown model requested by the adapter",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "the adapter-side rejection when a configured model is not in the provider's catalogue",
      "rationale": "the second ungated spelling (see model-unavailable-invalid-model). Without it the adapter's own wording is unpinned while the API's is pinned."
    },
    {
      "id": "model-unavailable-not-included-in-plan",
      "input": "claude-opus-4-5 is not included in your current plan",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic's plan-restriction wording",
      "rationale": "one of four interchangeable plan phrasings, each independently deletable, and all four gated on a registry model mention so an unrelated 'not included in your' sentence cannot trigger a tier downgrade. Only `not available on your` had a row."
    },
    {
      "id": "model-unavailable-not-offered-on-plan",
      "input": "claude-opus-4-5 is not offered on your plan",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic's plan-restriction wording",
      "rationale": "the third of the four plan phrasings (see model-unavailable-not-included-in-plan). The set exists because the provider's wording is not stable; an unpinned member is a downgrade that quietly stops firing."
    },
    {
      "id": "model-unavailable-not-supported-on-plan",
      "input": "claude-opus-4-5 is not supported on your plan",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic's plan-restriction wording",
      "rationale": "the fourth of the four plan phrasings (see model-unavailable-not-included-in-plan), pinned for the same reason."
    },
    {
      "id": "model-unavailable-usage-cap",
      "input": "Model usage cap reached for claude-opus-4-5; resets at 5pm",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic's model-specific usage-cap message",
      "rationale": "`usage cap` is the cap-phrase sibling of `usage limit` and `weekly limit`, and the only one with no row. Note the boundary of the declared session-usage-limit-quota extension: it claims `usage limit` and `session limit` and NOT `usage cap`, so this row's reaction side is empty even though its record is model_unavailable. The extension is deliberately no wider than the wording the pre-#306 ladder carried."
    },
    {
      "id": "model-unavailable-predicate-by-model-id",
      "input": "API Error: usage limit reached for gpt-5.6-terra",
      "expected": "model_unavailable",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "Anthropic-style cap wording naming a concrete registry id (#42). Written to exercise the mentions_registry_model predicate's ID branch specifically: this id contains no tier word, so the row fails if the ID lookup breaks even though the tier lookup still works.",
      "rationale": "Exercises the mentions_registry_model predicate through its ID branch: `gpt-5.6-terra` is a registry id, not a display name and not a tier, so deleting the id branch in either language turns this row red on its own. The record is model_unavailable; the reaction is the declared session-usage-limit-quota extension, which claims every `usage limit` line the signal subset does not, including this one."
    },
    {
      "id": "model-unavailable-predicate-by-display-name",
      "input": "Model usage cap reached for GPT-5.6 Sol on this account",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Provider cap wording naming a model by DISPLAY NAME (#42). Chosen so the display name shares no substring with any tier or id, isolating the predicate's display-name branch.",
      "rationale": "PREDICATE ROW, display-name branch. A provider that names the model the way a human would must still reach #42's tier downgrade rather than falling through to the generic path. Together with the id row above and the negative below, the three branches of mentions_registry_model are each pinned through the classifier in both languages, not just asserted directly."
    },
    {
      "id": "pr-merge-unmerged-negative-bare-reported-success",
      "input": "pr-merge reported success for issue #42; PR #77 merged cleanly",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator's post-merge verification wording with the SECOND half of the conjunction absent — the happy path it logs when the PR really did merge.",
      "rationale": "CONJUNCTION ROW. The pr-merge rule's third clause needs BOTH `pr-merge reported success` and `is not merged`; a stage that reported success and DID merge must classify as nothing at all. Splitting that clause into two — the round-2 finding that a literal diff could not see, because the literals are unchanged — makes this text a pr_merge_unmerged failure, which is the opposite of what happened."
    },
    {
      "id": "pr-merge-unmerged-negative-bare-is-not-merged",
      "input": "post-merge verification: PR #77 is not merged (state: OPEN)",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The same verification gate's wording with the FIRST half of the conjunction absent — e.g. a diagnostic emitted before pr-merge ran at all.",
      "rationale": "CONJUNCTION ROW, other half. `is not merged` on its own is ordinary English about a pull request and appears in text that has nothing to do with a pr-merge stage claiming success; only the pair means the stage lied about its outcome. Together with the row above, both halves of the clause are pinned, so widening the AND to an OR turns two rows red instead of merely showing a diff."
    },
    {
      "id": "permission-denied-negative-bare-tool-use-result",
      "input": "tool_use_result: {\"stdout\":\"ok\"} — the build step finished",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A Claude CLI result envelope's tool_use_result field on a SUCCESSFUL tool call, which appears in stage output constantly.",
      "rationale": "CONJUNCTION ROW. The permission_denied rule's last clause needs `tool_use_result` AND `rejected`; the field name alone is in almost every transcript. Loosening that pair would classify routine successful tool output as a harness denial and route real crashes to the short-backoff no-lifetime-cap path, which is the recovery for a denial and the wrong one for a crash."
    },
    {
      "id": "issue-closed-negative-bare-wrapper",
      "input": "[pipeline-start-failure] preflight aborted before any AI stage ran",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "HeadlessOrchestrator.preCheckAuth's wrapper prefix with no specific reason token — the shape every pipeline-start failure shares.",
      "rationale": "CONJUNCTION ROW for the three rules that key on the `pipeline-start-failure` wrapper (issue-closed, github-quota-low). The wrapper alone says only WHEN the run stopped, never WHY; a rule that claimed it would swallow every other pipeline-start failure into whichever of those kinds sits highest. This is also the negative that proves the wrapper is not a marker."
    },
    {
      "id": "github-quota-low-negative-legacy-wording-unwrapped",
      "input": "GitHub API quota too low: 8/5000 remaining",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The legacy descriptive wording of the quota preflight, quoted outside a pipeline-start failure — e.g. a doctor or preflight advisory line.",
      "rationale": "CONJUNCTION ROW. The legacy wording is matched only in conjunction with the `pipeline-start-failure` wrapper, because on its own it is a advisory sentence that any diagnostic can print; the stamped `github-quota-low` token is what a real preflight abort carries. Dropping the wrapper from that clause would let an informational line halt-exempt a run that failed for an unrelated reason."
    },
    {
      "id": "conjunction-negative-econnrefused-without-api-error",
      "input": "exit 1: [validation-failed] integration suite could not reach the local API: connect ECONNREFUSED 127.0.0.1:8787",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "skills/nightgauge-feature-validate stamps `[validation-failed]`; the ECONNREFUSED text is the Node client's own wording, wrapped by scheduler.SetStageError's `exit N: ` prefix.",
      "rationale": "The NEGATIVE half of `api error AND econnrefused`. A refused local port is an ordinary test failure, and api_connection_lost would route it to transient-blip recovery (short backoff, no lifetime-cap increment) instead of surfacing a real defect — which is exactly what that rule's own `why` says the conjunction prevents. Widen the clause to a bare `econnrefused` and this row flips to api_connection_lost."
    },
    {
      "id": "conjunction-negative-connection-refused-without-api-error",
      "input": "exit 1: [validation-failed] docker compose up postgres: dial tcp 127.0.0.1:5432: connect: connection refused",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "skills/nightgauge-feature-validate stamps `[validation-failed]`; the dial text is Docker's own wording under the `exit N: ` prefix.",
      "rationale": "The NEGATIVE half of `api error AND connection refused` — the exact widening a round-3 evasion performed with every suite green. A refused docker service is an environment problem for THIS repo's tests, not a dropped Anthropic connection, and the fleet must see the validation failure rather than a transient network blip."
    },
    {
      "id": "conjunction-negative-connection-reset-without-api-error",
      "input": "exit 1: git push failed: fatal: unable to access remote repository: Connection reset by peer",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "scheduler.SetStageError wrapping a git transport failure; `Connection reset by peer` is git/curl's wording.",
      "rationale": "The NEGATIVE half of `api error AND connection reset`. A reset against the git remote is not an Anthropic transport drop: it falls through to the crash fallback, where the operator sees a push failure. Widening the clause to a bare `connection reset` would relabel every git network hiccup as api_connection_lost."
    },
    {
      "id": "conjunction-negative-connection-closed-without-api-error",
      "input": "exit 1: [validation-failed] postgres: server closed the connection unexpectedly — connection closed while loading fixtures",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "skills/nightgauge-feature-validate stamps `[validation-failed]`; the wording is libpq's, under the `exit N: ` prefix.",
      "rationale": "The NEGATIVE half of `api error AND connection closed`, the clause #227 added for the live `API Error: Connection closed mid-response` wording. Without the conjunction, any database or proxy that closes a socket during a test run would be recorded as a model-API blip and retried instead of reported."
    },
    {
      "id": "conjunction-negative-getaddrinfo-eai-again-without-api-error",
      "input": "exit 1: npm error: request to https://redacted.invalid/ failed, reason: getaddrinfo EAI_AGAIN redacted.invalid",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "scheduler.SetStageError wrapping an `npm install` failure; the reason string is undici's.",
      "rationale": "The NEGATIVE half of BOTH `api error AND getaddrinfo` and `api error AND eai_again` — one input, because npm's DNS failure carries both tokens exactly as the resolver emits them. A registry outage during install is a stage failure to fix, not an Anthropic transport drop to retry silently."
    },
    {
      "id": "conjunction-negative-fetch-failed-without-api-error",
      "input": "exit 1: [validation-failed] playwright: TypeError: fetch failed — the dev server on port 5173 never came up",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "skills/nightgauge-feature-validate stamps `[validation-failed]`; `TypeError: fetch failed` is undici's message as Playwright surfaces it.",
      "rationale": "The NEGATIVE half of `api error AND fetch failed`. `fetch failed` is undici's generic message and appears in any Node test that talks to a local service, so unqualified it would swallow whole classes of real validation failures into the transient-network bucket."
    },
    {
      "id": "conjunction-negative-not-found-error-without-model",
      "input": "exit 1: gh api graphql: {\"type\":\"not_found_error\",\"message\":\"Could not resolve to a ProjectV2\"}",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "scheduler.SetStageError wrapping a board-sync failure; `not_found_error` is the GitHub GraphQL error type, which shares its spelling with the Anthropic 404 shape.",
      "rationale": "The NEGATIVE half of `not_found_error AND model`. Two different APIs use that token, and only the one that also names a model means the configured model is gone. Without the conjunction every 404 from the forge would book as model_unavailable and the scheduler would start rotating models to fix a missing project board."
    },
    {
      "id": "conjunction-negative-bare-model-word",
      "input": "exit 1: schema validation failed for the model context file",
      "expected": "validation_error",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates/validate stamps `schema validation failed`; the phrase `model context file` is ordinary prose about a pipeline artifact.",
      "rationale": "The NEGATIVE half of `not_found_error AND model`, and the one with the widest blast radius: `model` is an English word that appears in stall kills, cost-cap kills and validation errors alike, and the model-unavailable rule sits at rung 6 of 33 — above stall_kill, budget_exceeded, validation_error and subagent_crash. Widening the clause to a bare `model` moved 69 of 1,410 derived inputs in the round-3 review, and this row is what turns that red."
    },
    {
      "id": "conjunction-negative-plan-phrase-not-available-no-model",
      "input": "exit 1: API Error: extended prompt caching is not available on your current plan",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic plan-restriction wording as the CLI surfaces it, under scheduler.SetStageError's `exit N: ` prefix — with a FEATURE named rather than a model.",
      "rationale": "The NEGATIVE half of `not available on your AND @mentions_registry_model`. Anthropic uses this sentence for every plan restriction, most of which have nothing to do with the model: only the model-named variant means the pipeline must stop asking for that model. Without the predicate half, a caching or tier restriction would send the scheduler off rotating models."
    },
    {
      "id": "conjunction-negative-plan-phrase-not-included-no-model",
      "input": "exit 1: API Error: the batch API is not included in your plan",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic plan-restriction wording (the `not included in your` variant), under the `exit N: ` prefix, naming an API rather than a model.",
      "rationale": "The NEGATIVE half of `not included in your AND @mentions_registry_model`. Same argument as the sibling phrase and pinned separately because each wording is its own clause: deleting the predicate from any one of the four leaves the other three still gated, so one row per phrase is what makes each half red on its own."
    },
    {
      "id": "conjunction-negative-plan-phrase-not-offered-no-model",
      "input": "exit 1: API Error: priority tier throughput is not offered on your plan",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic plan-restriction wording (the `not offered on your` variant), under the `exit N: ` prefix, naming a throughput tier rather than a model.",
      "rationale": "The NEGATIVE half of `not offered on your AND @mentions_registry_model`. A throughput-tier restriction is an account fact the pipeline cannot act on by changing models, so it must fall through to the generic path rather than book as model_unavailable."
    },
    {
      "id": "conjunction-negative-plan-phrase-not-supported-no-model",
      "input": "exit 1: API Error: the files endpoint is not supported on your plan",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic plan-restriction wording (the `not supported on your` variant), under the `exit N: ` prefix, naming an endpoint rather than a model.",
      "rationale": "The NEGATIVE half of `not supported on your AND @mentions_registry_model`. Completes the four plan-phrase clauses: each is pinned from both sides, so neither the phrase nor the predicate can be dropped from any of them without a red row."
    },
    {
      "id": "conjunction-negative-usage-cap-no-model",
      "input": "exit 1: API Error: your organisation has reached its monthly usage cap",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Anthropic org-level cap wording under the `exit N: ` prefix — a cap on the ACCOUNT, with no model named.",
      "rationale": "The NEGATIVE half of `usage cap AND @mentions_registry_model`. An org-wide cap is not a statement about any particular model, so model_unavailable would be the wrong record and would send the scheduler rotating models against a limit that applies to all of them. Note this wording is deliberately NOT claimed by the session-usage-limit-quota extension either: `usage cap` is not one of its clauses."
    },
    {
      "id": "conjunction-negative-registry-model-named-in-a-stall-kill",
      "input": "exit 1: [stall-killed] feature-dev terminated: opus produced no output for 15m",
      "expected": "stall_kill",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "SkillRunner's `[stall-killed] ${stage} terminated` template, with the model tier named in the diagnostic tail.",
      "rationale": "The NEGATIVE half of the @mentions_registry_model term in ALL SIX model-unavailable clauses at once: naming a model is normal in failure text — stall diagnostics, cost lines and routing logs all do it — and it means nothing on its own. Drop any of those clauses' literal halves and this row flips from stall_kill to model_unavailable, because model-unavailable sits at rung 6 and stall-kill at rung 23."
    },
    {
      "id": "conjunction-negative-issue-closed-token-without-the-wrapper",
      "input": "exit 1: board sync failed: the issue-closed webhook handler returned 502",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "scheduler.SetStageError wrapping a board-sync failure whose message happens to name the `issue-closed` handler.",
      "rationale": "The NEGATIVE half of `pipeline-start-failure AND issue-closed`. The issue_closed kind means the run DEFERRED because the issue was already closed — a non-failure that books no lifetime failure. A webhook error that merely names the same token is a real failure, and without the wrapper half it would be silently written off as a deferral."
    },
    {
      "id": "conjunction-negative-fetch-first-without-push-rejected",
      "input": "exit 1: git pull --rebase: hint: you may want to fetch first and integrate the remote changes",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "scheduler.SetStageError wrapping a git failure; `fetch first` is git's own hint text, which appears on pulls as well as on rejected pushes.",
      "rationale": "The NEGATIVE half of `push rejected AND fetch first`. branch_forked is unrecoverable by retry and changes the recovery path, so it must require evidence that a PUSH was rejected — git prints the same hint in situations a retry does resolve."
    },
    {
      "id": "signal-extension-bare-usage-limit",
      "input": "success: You've hit your usage limit · resets 3pm",
      "expected": "",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "The Anthropic CLI's plain-text limit line as SkillRunner.extractTailError surfaces it on the non-stream-json path (skillRunner.ts quotes this shape verbatim).",
      "rationale": "THE DECLARED DIVERGENCE, from the side where it is load-bearing. No rule classifies a bare quota line with no model named, so the RECORD is empty and Go's NotifyComplete falls back to subagent_crash — a lifetime-failure increment and a cascade strike for a window that clears on its own. The REACTION is the session-usage-limit-quota extension, restoring #3792: applyQuotaCooldownLocked until the real bucket reset, board→Ready, RecordNonFaultOutcome. This row is also the negative half of `usage limit AND @mentions_registry_model`: widen that clause and the record flips to model_unavailable."
    },
    {
      "id": "signal-extension-bare-session-limit",
      "input": "You've hit your session limit · resets 10:30am (America/Denver)",
      "expected": "",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "The Anthropic CLI's session-limit line; the identical regex still lives in tokenParser.isAnthropicSessionLimit and in ConcurrentPipelineManager's halt policy.",
      "rationale": "The `session limit` clause of the extension, which the `usage limit` rows do not exercise. Same class as its sibling: an environmental quota window with no model named, invisible to the record ladder. Two other call sites in this repo still key on this exact wording, so a table that never heard of it would leave three components disagreeing about one shape."
    },
    {
      "id": "signal-extension-usage-limit-under-the-crash-wrapper",
      "input": "exit 1: usage limit",
      "expected": "subagent_crash",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "scheduler.SetStageError's `exit N: ` prefix around the shortest quota wording the CLI emits.",
      "rationale": "The extension firing while the record says something SPECIFIC and generic at once: `exit ` makes the crash fallback claim it, so the record is subagent_crash. That is the case the reviewer measured as the real cost of deleting the branch — a counted crash plus a cascade strike per quota window. Pinned here so the extension cannot be dropped without a red row on the exact shape that motivated it."
    },
    {
      "id": "signal-extension-codex-usage-limit",
      "input": "codex: usage limit reached for this account",
      "expected": "",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "The Codex CLI's account-limit wording, surfaced through SkillRunner.extractTailError (Codex has no stream-json result envelope to normalise, which is why the extension matters most here).",
      "rationale": "The non-Anthropic half of the same class. skillRunner normalises quota text to `[rate-limit-quota-exhausted]` only on the Claude stream-json result-envelope path, so for every other adapter the extension IS the routing. Deleting it silently converts each Codex quota window into a counted crash."
    },
    {
      "id": "boundary-negative-usage-limits-plural",
      "input": "exit 1: [validation-failed] the usage limits policy test is red",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate's `[validation-failed]` stamp (#326) around a test name that happens to contain the plural of the extension's wording.",
      "rationale": "THE WORD BOUNDARY, from the side that costs something. `usage limits` CONTAINS `usage limit`, so deleting the two-character `~` from the extension's term makes this fire — a run whose record says validation_failed would make the fleet react rate_limit_quota_exhausted, and that kind drives applyQuotaCooldownLocked: a cooldown across EVERY issue until an imaginary bucket resets. The pre-#306 rule was `/\\b(?:session|usage)\\s+limit\\b/i` precisely so this text did not route that way, and this row is what makes dropping the boundary red instead of invisible."
    },
    {
      "id": "boundary-negative-usage-limited",
      "input": "usage limited to 200 requests per hour for this token",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A forge/proxy throttling sentence — the wording an HTTP 403 body carries when a token is rate-shaped rather than exhausted.",
      "rationale": "The other suffix the plain-containment widening would claim, and the one main's `\\b` also rejected. Nothing classifies this text, so the honest answer is the unknown kind on both sides; if the boundary were dropped the record would stay empty while the reaction became a global quota cooldown, which is the exact record-vs-reaction split #306 exists to remove."
    },
    {
      "id": "boundary-negative-session-limits-plural",
      "input": "session limits are per-account, not per-workspace",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "Documentation/diagnostic prose about the limit policy, as it reaches the classifier through a stage's captured stderr tail.",
      "rationale": "The `session limit` half of the same guard: the extension has two clauses and each needs its own boundary negative, or dropping the `~` from one of them alone would still leave a green suite. Same cost as its sibling — a global quota cooldown for text that describes a policy rather than reporting a quota window."
    },
    {
      "id": "boundary-negative-github-usage-limits",
      "input": "Error: GitHub API usage limits exceeded for this token",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The GitHub REST error body as it surfaces through the forge client's error wrapping — a token-scoped limit, not the model provider's.",
      "rationale": "A boundary negative that is also a WRONG-PROVIDER negative. Under plain containment this GitHub message would route the fleet into an Anthropic/Codex quota cooldown, pausing every issue on a condition that belongs to one token. Kept as a row rather than left to the derived stress set because the stress set only ever composes the table's own vocabulary and would never produce this sentence."
    },
    {
      "id": "boundary-negative-usage-limit-double-space",
      "input": "You've hit your usage  limit - resets 3pm",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "The same Anthropic plain-text limit line as signal-extension-bare-usage-limit, with the separator widened — the shape skillRunner.extractTailError produces when the phrase is split across the last three non-empty lines it joins.",
      "rationale": "THE DISCLOSED NARROWING, pinned as a written expectation rather than left as an accident. main matched `/\\b(?:session|usage)\\s+limit\\b/i`, where `\\s+` is one-or-more whitespace; the restored term is a literal and requires exactly one space, so this input signals nothing and the run books a crash plus a cascade strike for a quota window that clears on its own. It is not closed because a whitespace-run term kind is a second matching semantic both interpreters would have to reproduce character-for-character. If one lands, this row flips — deliberately, with an argument, in the same diff."
    },
    {
      "id": "boundary-negative-left-edge-usage-limit-snake-case",
      "input": "exit 1: [validation-failed] max_usage limit assertion failed",
      "expected": "validation_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "feature-validate's `[validation-failed]` stamp (#326) around an assertion name whose snake_case identifier ends in the extension's first word.",
      "rationale": "THE LEFT EDGE OF THE WORD BOUNDARY, from the side that costs something — the mirror of boundary-negative-usage-limits-plural. `containsWordBounded` is a CONJUNCTION of two independent tests, and every other boundary row violates the RIGHT edge only, so the left conjunct (`!isWordByte(lowered, i-1)`) was exercised by nothing: deleting it was one contiguous deletion on one line in one file, in either language independently, with every suite green and the golden byte-identical. `max_usage limit` CONTAINS `usage limit` and `_` is a word character, so under the widened matcher this validation failure would make the fleet react rate_limit_quota_exhausted — applyQuotaCooldownLocked across EVERY issue until an imaginary bucket resets. main's `\\b` rejected it; this row is what makes the deletion red instead of invisible."
    },
    {
      "id": "boundary-negative-left-edge-usage-limit-bare",
      "input": "api_usage limit exceeded for this token",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A metering/proxy error naming the counter it tripped — the shape any producer emitting a snake_case identifier (api_usage, token_usage, data_usage, storage_usage) before ` limit` reaches the classifier with.",
      "rationale": "The unclaimed half of the same edge, and the reason the left conjunct cannot be dismissed as unreachable: the identifier-then-phrase shape is what real metering text looks like, and there are four spellings of it in this repo's own vocabulary. Nothing classifies this text, so the honest answer is the unknown kind on both sides; drop the left check and the record stays empty while the reaction becomes a global quota cooldown, which is precisely the record-vs-reaction split #306 exists to remove."
    },
    {
      "id": "boundary-negative-left-edge-session-limit",
      "input": "Error: 403 forbidden; browser_session limit exceeded for this workspace",
      "expected": "",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A forge/proxy HTTP 403 body about concurrent browser sessions, as it surfaces through the forge client's error wrapping.",
      "rationale": "The `session limit` half of the left-edge guard: the extension has two `~terms` and each needs its own negative on each edge, or deleting the left conjunct while only `usage limit` is pinned would still leave a green suite for one of them. Same cost as its siblings — a provider-wide quota cooldown for a message about one workspace's concurrent sessions, which main's `\\b(?:session|usage)\\s+limit\\b` never matched."
    },
    {
      "id": "order-signal-rule-beats-extension-adapter-auth",
      "input": "exit 1: [adapter-auth-failed] the cli reports you've hit your usage limit",
      "expected": "adapter_auth_failed",
      "expected_signal": "adapter_auth_failed",
      "source": "synthetic",
      "producer": "The pipeline-start auth gate's `[adapter-auth-failed]` stamp (#312) wrapped by scheduler.SetStageError, around the adapter CLI's own explanation of why the probe failed.",
      "rationale": "THE STAGE ORDER OF THE SIGNAL PROJECTION, pinned as behaviour. Nine documents state that an extension can never overrule a kind projected by a `signal: true` RULE, and what enforces it is that SignalKind consults the rule ladder BEFORE the extensions — a four-line reorder with no literal, no import and no artifact movement. This input carries both a signal marker and the extension's wording, which nothing in the corpus or the derived set did before, so swapping the two blocks turns the reaction into rate_limit_quota_exhausted while the record still says adapter_auth_failed: a fleet-wide quota cooldown for a logged-out adapter, which a retry would have fixed in seconds. The record and the reaction must both name the rule."
    },
    {
      "id": "order-signal-rule-beats-extension-api-overloaded-wording-first",
      "input": "You've hit your usage limit · resets 3pm\nAPI Error: Overloaded",
      "expected": "api_overloaded",
      "expected_signal": "api_overloaded",
      "source": "synthetic",
      "producer": "SkillRunner.extractTailError joining the last non-empty lines of a Claude CLI session, where a quota notice printed earlier is followed by the 529 that actually ended the stage.",
      "rationale": "The same ordering claim with the two phrases the OTHER way round, because the bound is about the interpreter's stage order and not about which phrase the text mentions first. Matching is containment, so a reader must be able to see that the answer does not move when the wording does; and a row where the extension's phrase comes first is the one a reader would otherwise expect the extension to win. api_overloaded is a transient blip with a short backoff — routing it to rate_limit_quota_exhausted instead would park every issue for the length of a quota window on a 529 that clears in seconds."
    },
    {
      "id": "stage-context-unreadable-dev",
      "input": "stage gate failed: failed to read dev context file",
      "expected": "stage_context_unreadable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates.FeatureDevGate's read-error branch, wrapped by internal/orchestrator/scheduler.go as `stage gate failed: <reason>`",
      "rationale": "The exact wrapped text the scheduler emits when a gate cannot read the stage's own context for a reason other than absence (#1237). Before the sweep no rule matched the `stage gate failed:` wrapper at all, so the generic `exit ` fallback booked a filesystem fault as a subagent crash."
    },
    {
      "id": "stage-context-unreadable-plan-file",
      "input": "stage gate failed: failed to stat plan_file",
      "expected": "stage_context_unreadable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates.FeaturePlanningGate's plan_file stat-error branch, wrapped by the scheduler",
      "rationale": "The second clause shape of the same kind: planning's plan_file exists as far as the context says but cannot be stat'ed (ENOTDIR, EACCES). Pinned separately so a clause deleted for `failed to stat` goes red on its own."
    },
    {
      "id": "dev-build-verification-missing",
      "input": "stage gate failed: dev context lacks build_verification — the dev completion contract requires the verification step (nightgauge build run)",
      "expected": "dev_build_verification_missing",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates.FeatureDevGate check 3 (#55), wrapped by the scheduler",
      "rationale": "The skill skipped its verification step (#1237). An agent-behaviour failure that a crash-recovery path cannot fix; it needs the stage to re-run its contract, not a retry of a process that never died."
    },
    {
      "id": "dev-build-verification-failed",
      "input": "stage gate failed: dev context records build_verification.status=failed",
      "expected": "dev_build_verification_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates.FeatureDevGate check 4, wrapped by the scheduler",
      "rationale": "The stage's build broke (#1237) — organic, like validation_failed, and the record must say so or the failure weighting charges an infrastructure crash to the reliability score."
    },
    {
      "id": "dev-tests-failed",
      "input": "stage gate failed: dev context records failing tests",
      "expected": "dev_tests_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates.FeatureDevGate check 5 (#55), wrapped by the scheduler",
      "rationale": "The stage's own tests failed (#1237). Same family as the build case, kept distinct so a triager knows whether to look at the build or the suite."
    },
    {
      "id": "pr-merge-lookup-failed",
      "input": "stage gate failed: gh pr view failed after retries and local git fallback found no merge commit",
      "expected": "pr_merge_lookup_failed",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gates.PrMergeGate's gh-and-git-both-failed branch, wrapped by the scheduler",
      "rationale": "The gate could not see the PR (#1237) — gh failed or was rate-limited on every attempt and local git found no merge commit. Infrastructure: the merge may have landed. Must not read as pr_merge_unmerged (a PR the gate saw open) nor as a crash."
    },
    {
      "id": "multiline-claude-headless-socket-drop-after-retry-notice",
      "input": "exit 1: Retrying request to /v1/messages in 2000ms\nAPI Error: The socket connection was closed unexpectedly",
      "expected": "api_connection_lost",
      "expected_signal": "api_connection_lost",
      "source": "synthetic",
      "producer": "claude-headless (internal/execution/adapters/claude.go, registry.go factory \"claude-headless\") — the Claude CLI's own SDK retry log line followed by its transport-drop wording (#4002), reaching the classifier as raw stderr via stderrFailureReason's last-3-lines join, wrapped in the scheduler's `exit N: ` prefix (#565 AC1's input_contract).",
      "rationale": "Multi-line adapter-authored coverage for the claude-headless constructor (#565 AC2): a benign retry-backoff line the CLI prints to stderr before the fatal transport drop must not stop the real error's own wording (`socket connection was closed`) from being seen and claimed by api-connection-lost. Above the crash rules so a seconds-long blip is not misread as a process death."
    },
    {
      "id": "multiline-claude-sdk-agent-sdk-traceback-overloaded",
      "input": "exit 1: Traceback (most recent call last):\n  File \"agent_sdk/client.py\", line 214, in _stream\nanthropic.APIStatusError: Error code: 529 - Overloaded",
      "expected": "api_overloaded",
      "expected_signal": "api_overloaded",
      "source": "synthetic",
      "producer": "claude-sdk (internal/execution/adapters/claude_sdk.go, registry.go factory \"claude-sdk\") — a Python Agent SDK traceback ending in Anthropic's 529 (#3835 WS4), reaching the classifier as raw stderr rather than the CLI's stream-json result envelope, because the SDK path never produces one.",
      "rationale": "Multi-line adapter-authored coverage for the claude-sdk constructor (#565 AC2): the SDK surfaces the same 529 as the headless CLI but shaped as a stack trace, with the traceback frame lines carrying no table term at all — only the last line does. `overloaded` is distinctive to this API error and must win over the stall-kill/crash rules so a momentary capacity blip gets a short backoff, not a lifetime-failure increment."
    },
    {
      "id": "multiline-codex-usage-limit-no-model-named",
      "input": "[codex] retrying tool call after backoff\ncodex: usage limit reached for this account",
      "expected": "",
      "expected_signal": "rate_limit_quota_exhausted",
      "source": "synthetic",
      "producer": "codex (internal/execution/adapters/codex.go, registry.go factory \"codex\") — the Codex CLI's own quota wording preceded by an unrelated retry-progress line, reaching the classifier as raw stderr (no skillRunner bracketed stamp exists on this path).",
      "rationale": "Multi-line adapter-authored coverage for the codex constructor (#565 AC2), and a genuine exercise of the session-usage-limit-quota signal extension on a realistic multi-line tail: no rule above names a model, so the record ladder answers nothing and the extension is the ONLY thing standing between this text and subagent_crash. The leading `[codex] retrying tool call after backoff` line is deliberately inert — it carries no table term — to show the extension's `~usage limit` boundary triggers on the real second line regardless of what came before it."
    },
    {
      "id": "multiline-gemini-model-not-found-after-catalog-fetch",
      "input": "exit 1: Fetching model catalog...\nError: model not found: gemini-3.0-ultra-preview",
      "expected": "model_unavailable",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gemini (internal/execution/adapters/gemini.go, registry.go factory \"gemini\") — the Gemini CLI's own model-lookup failure (#42) preceded by an inert progress line, reaching the classifier as raw stderr.",
      "rationale": "Multi-line adapter-authored coverage for the gemini constructor (#565 AC2): the `Fetching model catalog...` line is real CLI chatter with no table term, and the actual failure (`model not found`) sits alone on the last line. Pins that a benign preceding line cannot suppress the specific model-unavailable match that follows it."
    },
    {
      "id": "multiline-gemini-sdk-adc-credentials-error-falls-through",
      "input": "exit 1: Loading application default credentials\ngoogle.auth.exceptions.DefaultCredentialsError: Could not automatically determine credentials. Please set GOOGLE_APPLICATION_CREDENTIALS or explicitly create credentials and re-run the application.",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "gemini-sdk (internal/execution/adapters/gemini_sdk.go, registry.go factory \"gemini-sdk\") — Google's real, well-known Application Default Credentials error text, reaching the classifier as raw stderr with no bracketed marker of any kind.",
      "rationale": "Multi-line adapter-authored coverage for the gemini-sdk constructor (#565 AC2), and a DELIBERATE negative finding: no rule in the table recognises Google's ADC credential wording (it says nothing like `not signed in`), so this real vendor shape falls all the way through to the generic `exit ` fallback and records subagent_crash. That is the correct, intended behaviour for an unrecognised reason (table.go's package doc: no match anywhere yields the empty kind and callers fall back to the most generic kind) — not a bug to fix here, but worth pinning so a future reader does not mistake the silence for an oversight."
    },
    {
      "id": "multiline-ollama-local-daemon-connection-refused",
      "input": "exit 1: Connecting to http://localhost:11434 ...\nError: dial tcp 127.0.0.1:11434: connect: connection refused",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "ollama (internal/execution/adapters/ollama.go, registry.go factory \"ollama\") — a realistic local-daemon-not-running failure. The ollama bridge is non-agentic and its execution bottoms out in the TypeScript fetch/SSE adapters (adapter.go's Agentic doc), but the reason text still reaches Classify through the same stderr path.",
      "rationale": "Multi-line adapter-authored coverage for the ollama constructor (#565 AC2): `connection refused` alone does NOT satisfy api-connection-lost, which gates that alternative on the co-occurring literal `api error` specifically so an unrelated infrastructure refusal does not misclassify as an Anthropic transport drop (the rule's own `why`). A bare Go `dial tcp ... connect: connection refused` is exactly the unrelated shape that gate exists to keep out, so this correctly falls to the generic fallback."
    },
    {
      "id": "multiline-lm-studio-local-server-econnrefused",
      "input": "exit 1: POST http://localhost:1234/v1/chat/completions\nError: connect ECONNREFUSED 127.0.0.1:1234",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "lm-studio (internal/execution/adapters/lmstudio.go, registry.go factory \"lm-studio\") — a realistic local LM Studio server failure, same non-agentic bridge shape as ollama.",
      "rationale": "Multi-line adapter-authored coverage for the lm-studio constructor (#565 AC2): mirrors multiline-ollama-local-daemon-connection-refused for the sibling bridge. `econnrefused` alone does not satisfy api-connection-lost's `[\"api error\", \"econnrefused\"]` alternative because the request line naming the local port is not an Anthropic `API Error:` envelope, so this correctly falls to the generic fallback rather than being misread as an Anthropic transport drop."
    },
    {
      "id": "multiline-copilot-gh-secondary-rate-limit",
      "input": "exit 1: gh: You have exceeded a secondary rate limit for the GitHub API\nPlease wait a few minutes before you try again",
      "expected": "github_rate_limited",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "copilot (internal/execution/adapters/copilot.go, registry.go factory \"copilot\") — `gh`'s own real secondary-rate-limit wording, which the Copilot CLI wraps.",
      "rationale": "Multi-line adapter-authored coverage for the copilot constructor (#565 AC2), and the row that #1391 was filed against: it used to assert subagent_crash, documenting today's behaviour rather than the correct one. `gh`'s own secondary-rate-limit wording is a TRANSIENT GitHub throttle that clears within minutes, and subagent_crash is a LIFETIME kind, so the pipeline abandoned a run a short backoff would have recovered. It is not github_quota_low: that rule is keyed to Nightgauge's own GraphQL board-budget preflight marker (`[pipeline-start-failure] github-quota-low`), a different producer whose recovery waits on the PRIMARY bucket's published reset — the wrong clock for a secondary limit."
    },
    {
      "id": "gh-secondary-rate-limit-http-403",
      "input": "exit 1: HTTP 403: You have exceeded a secondary rate limit. Please wait a few minutes before you try again. (https://api.github.com/graphql)",
      "expected": "github_rate_limited",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "`gh` surfacing GitHub's HTTP 403 secondary-limit body verbatim, the shape a GraphQL board read hits.",
      "rationale": "The same throttle as the copilot row, reached through the raw HTTP wrapper rather than gh's own prose. Pins that the rule keys on GitHub's stable `secondary rate limit` phrase and not on the `gh:` prefix, so a throttle raised by any surface that shells out to the API classifies the same way."
    },
    {
      "id": "gh-api-rate-limit-exceeded-primary",
      "input": "exit 1: gh: API rate limit exceeded for user ID 12345.",
      "expected": "github_rate_limited",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "`gh`'s PRIMARY rate-limit wording, emitted mid-stage when the hourly bucket empties during a run.",
      "rationale": "The primary-bucket sibling of the secondary limit, and it fell to subagent_crash for exactly the same reason. It shares the kind because the recovery is the same shape — back off and retry, never a lifetime failure — and because the alternative, github_quota_low, is the PREFLIGHT's marker: routing a mid-stage exhaustion there would apply a global cooldown read off a tracker snapshot the failing run never refreshed."
    },
    {
      "id": "gh-submitted-too-quickly",
      "input": "exit 1: gh: You have exceeded a secondary rate limit and have been temporarily blocked from content creation. Please retry your request again later.\nwas submitted too quickly",
      "expected": "github_rate_limited",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "GitHub's content-creation secondary-limit wording, seen when pr-create opens PRs in quick succession.",
      "rationale": "GitHub uses distinct prose for the content-creation secondary limit, which is precisely the one an autonomous pipeline trips: several runs opening PRs within a minute. Pinned as its own row because the `was submitted too quickly` clause is the only thing that catches the wording when the leading line is truncated away by the 3-line stderr tail."
    },
    {
      "id": "gh-http-429-too-many-requests",
      "input": "exit 1: HTTP 429: Too Many Requests (https://api.github.com/repos/nightgauge/nightgauge/pulls)",
      "expected": "github_rate_limited",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "GitHub returning 429 rather than 403 for a throttled REST call, surfaced by gh as an HTTP error.",
      "rationale": "GitHub answers some throttles with 429 instead of 403, and gh prints the status line with the API URL. Both conjunctive clauses (`too many requests`+`github`, `http 429`+`github`) are satisfied here; the bare-half rows below are what prove the `github` conjunct is load-bearing rather than decorative."
    },
    {
      "id": "negative-too-many-requests-without-github",
      "input": "exit 1: upstream proxy replied: too many requests, slow down",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A non-GitHub vendor emitting the same generic phrase — the false positive the conjunction exists to prevent.",
      "rationale": "`too many requests` is a shape any HTTP client prints, and the input contract says adapter stderr reaches Classify exactly as the vendor wrote it. Widening the clause to that term alone would move this row off subagent_crash, which is what makes the `github` conjunct provably necessary rather than incidental."
    },
    {
      "id": "negative-http-429-without-github",
      "input": "exit 1: model gateway returned http 429 after 3 retries",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "A non-GitHub gateway printing the bare HTTP status — the other half of the same false positive.",
      "rationale": "The same argument as the `too many requests` control, for the second conjunctive clause. A 429 from an inference gateway is not a GitHub throttle and must not be routed to a GitHub backoff; widening `http 429` to fire alone would move this row and the conjunction test says so."
    },
    {
      "id": "negative-github-without-throttle-wording",
      "input": "exit 1: gh: failed to run git: could not read from remote repository github.com",
      "expected": "subagent_crash",
      "expected_signal": "",
      "source": "synthetic",
      "producer": "An ordinary `gh` failure that merely names github — the half that would swallow the world if widened.",
      "rationale": "`github` appears in a large share of this pipeline's failure text, so widening either conjunction to that term alone would claim almost every gh failure as a throttle. This row pins that half for BOTH conjunctive clauses, and is the reason the rule cannot quietly become a disjunction with every suite still green."
    },
    {
      "id": "multiline-grok-hard-cap-notice-then-not-signed-in",
      "input": "exit 1: Increasing per-request retry hard cap to 5 attempts\nError: Not signed in. To authenticate without a browser, run: grok login --device-code.",
      "expected": "adapter_auth_failed",
      "expected_signal": "adapter_auth_failed",
      "source": "synthetic",
      "producer": "grok (internal/execution/adapters/grok.go, registry.go factory \"grok\") — the grok Build CLI's real `Not signed in` auth wording (#591), preceded by an unrelated retry-configuration notice that happens to contain the bare literal `hard cap`.",
      "rationale": "THE #565 AC3 NEGATIVE CONTROL. Matching is over the WHOLE joined string, not per line (table.json's input_contract), so a rule's bare literal can be satisfied by an EARLIER, unrelated line even when a LATER line names the real, more specific failure. Before #565 this input classified stall_kill: stall-kill's bare `hard cap` clause sat at ladder position 23 while adapter-auth-failed's `not signed in` sat at position 26, so the retry-configuration noise line won over the actual auth failure that ended the stage. #565 moved adapter-auth-failed to directly follow rate-limit-quota-exhausted, grouping it with the OTHER rule that exists specifically to classify raw, unmarked vendor CLI stderr (#560/#591 are siblings), which is what this row now pins as passing. A retry-loop mentioning a hard cap is not itself a stall; a CLI that never authenticated is never going to retry its way out, and reporting stall_kill sent it down a backoff-and-retry recovery path for a failure retrying cannot fix."
    }
  ]
}
