# CI Integration

## Overview

This document describes the platform's CI configuration.

## Required Checks

The following GitHub status checks are required on the `main` branch:

- `build` — runs lint + test on every PR
