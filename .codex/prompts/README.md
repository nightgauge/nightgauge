# Legacy Codex Prompts

This directory is retained for backward compatibility only.

Current Codex slash commands should be defined in `.codex/commands/` and
installed via:

```bash
./scripts/install-codex-assets.sh
```
