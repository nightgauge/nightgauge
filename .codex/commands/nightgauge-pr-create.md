Run the Nightgauge PR creation stage using the arguments that follow this slash
command.

Steps:

1. If no issue number is provided, ask for one.
2. Run `scripts/run-stage.sh codex pr-create` with the provided arguments.
3. Summarize results and suggest `/nightgauge-pr-merge` next.
