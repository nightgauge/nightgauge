# Nested-Reference Child Fixture

> **Do not edit.** The leaf target of `nested-ref-fixture.md`. It exists so the
> nested-reference negative test exercises a real two-level chain
> (SKILL.md → nested-ref-fixture.md → this file). This leaf contains no further
> reference, so it must NOT itself trigger a finding.

This is the leaf of the chain. No further supporting-file references here.
