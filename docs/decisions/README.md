# Architecture Decision Records (ADRs)

This directory contains strategic decision documents for Nightgauge.

## Purpose

Unlike the operational documentation in `docs/` (ARCHITECTURE.md,
GIT_WORKFLOW.md, etc.), these documents record **strategic decisions** and the
analysis that led to them.

## Format

Each decision document follows the pattern:

- `NNN-title-of-decision.md` (e.g., `001-jira-vs-github-projects.md`)
- Date, author, status (Discovery, Decided, Implemented)
- Options analyzed with pros/cons
- Recommendation and rationale
- Implementation tracking (issues, milestones)

## Active Decisions

| ID  | Title                                       | Status    | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | ------------------------------------------- | --------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001 | Jira vs GitHub Projects Integration         | Discovery | 2026-02-04 | TBD - Leaning toward Option 1 (GitHub Projects maturity)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 002 | API Technology Selection                    | Decided   | 2026-02-21 | tRPC v11 with Hono — zero-codegen type safety for TypeScript client/server                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 004 | Adaptive Stall Recovery                     | Decided   | 2026-04-25 | Rewind to feature-planning once on first stall-kill (Issue #3005)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 007 | Slash-Command → Skill Invocation Contract   | Decided   | 2026-05-09 | Canonical enforcement banner that instructs agents to invoke the Skill tool before reading command file content (Issue #3343, Epic #3342)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 008 | Skills target the `nightgauge forge` CLI    | Decided   | 2026-05-11 | Skills migrate from direct `gh` calls to `nightgauge forge` (15 skills in Wave 4); deprecation linter gates regressions; carve-outs route through `forge graphql` (Issue #3363, Epic #3349)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 012 | Performance Modes → Policy Envelopes        | Proposed  | 2026-07-10 | Modes become floor/ceiling envelopes (router runs in every mode; frontier ceiling = Fable-on-L/XL only) + a Custom per-stage selector over existing `stage_models` config                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 013 | Run Lifecycle Trace Schema                  | Decided   | 2026-07-17 | Per-run append-only decision trace at `.nightgauge/pipeline/trace/<run_id>.jsonl`; envelope + closed kind taxonomy with structured rationale; `(run_id, producer, seq)` idempotent upload; dedicated platform trace-events table (Issue #179, Epic #178)                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 014 | Live Trace Transport (SSE run stream)       | Decided   | 2026-07-17 | Reuse the existing per-run SSE stream `GET /v1/pipelines/{runId}/stream`; push each ADR-013 trace envelope as a `trace.event` PipelineProgressEvent, idempotent by (run_id,producer,seq) (#234, Epic #226)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 015 | DecisionRequests (Action Center)            | Decided   | 2026-07-19 | First-class `DecisionRequest` primitive: closed `kind`/verb-registry schema, local-first `.nightgauge/attention/` store with a single authoritative writer, a `list`/`subscribe`/`resolve` Surface contract (VSCode/dashboard/future Discord), steer-as-context, trace+history audit (#323, Epic #322). **Amended 2026-07-25** (Decisions L/M, Epic #88): repo-scoped attention — the `attention sweep` evaluation loop with no run in flight, standing-condition semantics (sticky identity, fingerprint, mute-until-changed), and `auto_resolved` as a terminal state distinct from a human resolution                                                                                                                |
| 016 | Model-Aware Skill Overlays                  | Decided   | 2026-07-25 | Base `SKILL.md` stays the single procedural source of truth; per-provider/per-model variance ships as additive overlay fragments (`_shared/_overlays/` + per-skill `_overlays/`) composed by `nightgauge skill render`, keyed off the model registry (provider → concrete id since the #582 amendment), composer and `behavior` block shipped (#78, #79); the preflight gate, the shared-overlay corpus, and the eval evidence remain open (#80-#84). Operational guide: [MODEL_ADAPTATION.md](../MODEL_ADAPTATION.md)                                                                                                                                                                                                  |
| 017 | Runtime Identity Keying                     | Decided   | 2026-08-08 | The IPC runtime registry keys on a client-minted UUIDv7 `run_id` (which _is_ `RuntimeState.RunID` and _is_ the #307 dispatch generation), re-asserted on every `pipeline.*` call, adopted-on-sight when unknown and refused only when provably closed; destructive writes become claim + compare-and-delete with the identity in the snapshot filename (`runtime-{issue}-{runId}.json`); the issue number is demoted to a derived index (#370)                                                                                                                                                                                                                                                                          |
| 018 | Adapter Usage & Quota Model                 | Decided   | 2026-08-17 | One `UsageSnapshot` shape every usage surface reads, a pluggable `UsageProvider` registry resolved against the configured adapter, and `LocalTelemetryUsageProvider` deriving session/daily/monthly dollar windows from persisted per-stage cost; no adapter reports a quota, so every `limit` is the locally configured budget and "cannot say" is always `plan.kind: "unknown"` with no windows (#658)                                                                                                                                                                                                                                                                                                                |
| 019 | Relayed Resolve Routing                     | Decided   | 2026-09-04 | The platform addresses a relayed Action Center resolve to a per-MACHINE agent identity while the attention store is per-WORKSPACE, so a daemon can be handed a card it does not own: the non-owner classifies the misroute (`attention.ErrRequestNotFound` → `platform.ErrRelayedRequestNotHere`), names its own workspace root in the log, writes nothing across roots and still acknowledges; cross-root writes and serve-registry discovery are rejected, and per-workspace agent identity is deferred to a platform-side change (#1421)                                                                                                                                                                             |
| 020 | Value-Adding Features Default ON            | Decided   | 2026-09-06 | A feature that adds value to a workspace ships enabled; an opt-out exists for repo footprint and per-run token cost and never to hide value, and a default-off flag must record its reason beside the flag. `knowledge.enabled` flips to `true` at every layer, with "key absent" meaning the default rather than `false` — the falsy-test idioms (`!config.enabled`, `// false`, `get("enabled", false)`) were the actual bug (#1513)                                                                                                                                                                                                                                                                                  |
| 021 | Shipped Defaults Follow the Default-On Rule | Decided   | 2026-09-06 | Eight groups of shipped defaults re-decided against ADR-020: sanitization blocks, scope-drift and context budgets enforce, eval-advice routing and router auto-tune are on, cross-project complexity and knowledge aggregation are on, gate relaxation ships `[docs_only, config_only]`, forge-side auto-merge is off and pinned off in the committed public-core config, and `audit.enabled` follows `platform.enabled` instead of being an independent switch. "Conservative rollout" and "avoid breaking existing pipelines" are migration reasons, not footprint or cost, and never a default. `knowledge.index_on_commit` and `ralph_loop.lint` gated nothing and are deleted (#1519)                              |
| 022 | OpenCode Multi-Provider Adapter             | Decided   | 2026-09-12 | `opencode` is one adapter for many providers: the provider comes per dispatch from the `-m` id, normalized from the injected provider block. Experimental: `RunStage` refuses every dispatch unless `NIGHTGAUGE_EXPERIMENTAL_OPENCODE=1` is in the environment, and each allowed one lists the unenforced controls. Prompt on stdin only, no auto-approve, share or mDNS flag, a random server password per spawn. Only a model known to run on its endpoint is stamped `cost_usd: 0`, hosted cost comes from the registry, `anthropic/*` needs `ANTHROPIC_API_KEY`, each run keeps OpenCode's state in a private per-run root, and endpoints are named by id only. Amends ADR-016 and ADR-018, extends ADR-020 (#1612) |
| 023 | Model-Aware Context Budgets                 | Decided   | 2026-09-21 | `internal/skillrender/budget.go`'s pure `Fit` checks a rendered stage's estimated tokens (bytes/4, +15% margin) against a per-stage share of the model's window; `nightgauge skill render --context-window N` reports the verdict, byte-identical without the flag. At dispatch, the scheduler re-routes once to the same provider's largest-window model or refuses via the pre-existing, now-first-used `TerminalKindContextWindowExceeded`. `AutoProviderRouter` scores `opencode` by the resolved model's window instead of a static placeholder. Unknown windows fail open. Amends ADR-010 and ADR-016, extends ADR-022 and ADR-020 (#1645, Epic #1610)                                                            |

## Reference

Decision documents are referenced from implementation issues but are **not**
included in CLAUDE.md or AGENTS.md. They are historical records, not operational
guidance.

For operational guidance, see:

- [docs/ARCHITECTURE.md](../ARCHITECTURE.md) - How the system works
- [docs/GIT_WORKFLOW.md](../GIT_WORKFLOW.md) - How to use git
- [CLAUDE.md](../../CLAUDE.md) - Project instructions for AI agents
