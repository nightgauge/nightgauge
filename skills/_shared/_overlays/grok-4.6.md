Grok 4.6 is the default Grok Build model. Thinking is on by default;
keep `--effort` at the resolved Nightgauge envelope rather than
stacking extra "think harder" instructions. Verification and
delegation propensity are high — do not add redundant verify-your-work
scaffolding or extra review subagents on top of the stage contract.
Prefer one thorough pass over narrating every tool call.
