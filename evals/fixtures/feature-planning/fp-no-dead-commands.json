{
  "haiku": {
    "text": "The plan's preflight step relies only on real Go binary subcommands: `project sync-status` for board state and `knowledge read` for prior context. I verified each subcommand exists before referencing it, so the plan does not call any nonexistent reconcile preflight.",
    "exit_code": 0
  },
  "sonnet": {
    "text": "The plan's preflight step relies only on real Go binary subcommands: `project sync-status` for board state and `knowledge read` for prior context. I verified each subcommand exists before referencing it, so the plan does not call any nonexistent reconcile preflight.",
    "exit_code": 0
  },
  "opus": {
    "text": "The plan's preflight step relies only on real Go binary subcommands: `project sync-status` for board state and `knowledge read` for prior context. I verified each subcommand exists before referencing it, so the plan does not call any nonexistent reconcile preflight.",
    "exit_code": 0
  }
}
