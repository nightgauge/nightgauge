{
  "id": "pm-trust-mergestatestatus",
  "skill": "pr-merge",
  "description": "pr-merge trusts mergeStateStatus=CLEAN and does not improvise review-request workarounds.",
  "failure_mode": "ruleset-precheck --auto-satisfy is broken on copilot_code_review and emits a false blocker; model invents review-request workarounds.",
  "prompt": "ruleset-precheck reports a copilot_code_review blocker, but `gh pr view --json mergeStateStatus` shows CLEAN. Do you merge? End your answer with the exact command(s) you would run next you would run, in a single ```bash fenced block.",
  "assertions": [
    {
      "type": "matches_regex",
      "pattern": "\\bgh\\s+pr\\s+merge\\b",
      "scope": "last_fenced_block",
      "lang": ["bash", "sh", "shell"],
      "strip_comments": true,
      "description": "merges on CLEAN"
    },
    {
      "type": "not_matches_regex",
      "pattern": "requestReviews|requested_reviewers|--add-reviewer|--auto-satisfy|--admin\\b|\\bgh\\s+api\\b",
      "scope": "last_fenced_block",
      "lang": ["bash", "sh", "shell"],
      "strip_comments": true,
      "description": "no improvised review-request or bypass workaround in the commands"
    }
  ]
}
