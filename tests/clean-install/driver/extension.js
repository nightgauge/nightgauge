"use strict";
// The driver contributes nothing. The work happens in driver.js, which VS Code
// loads through --extensionTestsPath after the window is up.
function activate() {}
function deactivate() {}
module.exports = { activate, deactivate };
