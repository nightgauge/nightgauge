{
  "name": "clean-install-fixture",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny string-utils package used to seed the clean-install release gate's throwaway repository.",
  "license": "Apache-2.0",
  "main": "src/index.js",
  "scripts": {
    "build": "node --check src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
