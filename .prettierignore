node_modules/
dist/
build/
*.generated.ts
# Transient git worktrees created by parallel agents and the autonomous pipeline
.claude/worktrees/
.worktrees/
.nightgauge/
api/generated/
skills/*/dist/
packages/*/dist/
coverage/
*.vsix
*.lock
package-lock.json
# Test fixtures that may contain intentionally malformed content
tests/fixtures/
# The large VSCode index fixture is committed MINIFIED on purpose (#356): that
# is the shape VSCode itself writes, and it is the worst case for guard.sh's
# text extraction. Pretty-printing it would change what the test exercises.
internal/doctor/testdata/vscode-bundles/extensions-index-large.json
# The pre-fix skill render (#337) is a verbatim capture of what the model was
# handed. Reformatting it would rewrite the artifact under test.
internal/skillrender/testdata/dead-include/pr-merge.pre-fix.rendered.md
# Embedded templates emitted byte-for-byte by `nightgauge setup scaffold-tooling`
internal/setup/templates/
# Markdown files with template syntax (Handlebars, etc.)
claude-plugins/docs/
# Handlebars templates
*.handlebars
*.hbs

# Generated test snapshots — never prettier-formatted
**/__snapshots__/
